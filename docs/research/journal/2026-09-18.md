# 2026-09-18 Trading Journal

## 08:45 IST standup

Trading day confirmed — the only September NSE holiday on the calendar is 2026-09-14
(Ganesh Chaturthi). OpenAlgo is up (it holds an exclusive lock on
`db/historify.duckdb`, WAL written 08:51 IST).

### Yesterday's deltas

- **0 commits on 2026-09-17.** Branch `dev`, HEAD `cf767c202`.
- Most recent work landed 2026-09-16:
  - `cf767c202` Merge PR #731 from `feat/730-open15-watched-quote-poll`
  - `76430b246` [#730] feat(open15): price the watched-break counterfactual on the risk
    monitor's batched quote poll — live at the 09:30 exit, bars as fallback
- Nothing labelled `WIP`, `fix(test):` or `refactor:` in the recent window.

### Working tree state

115 entries in `git status --porcelain`, but only **2 tracked modifications**:

- ` M .gitignore`
- ` M strategies/simplified_engine/LEARNINGS.md` ← ⚠ per CLAUDE.md, strategy-doc
  changes go **direct to dev**. This one is sitting uncommitted.

The other 113 are untracked and expected: `backtest/` scratch (options_open15,
open15_rolling, open15_missed_days, inhouse_scanner/r60), `db/*.bak.*` snapshots
from the #626/#715/#728 repairs, and the accumulated
`docs/research/journal/*.md` files this standup writes and never commits.

### Pre-market freshness

`historify.duckdb` could not be read directly — the live OpenAlgo process holds it
exclusively (read returns EACCES from the sandbox). Freshness therefore read from
`data_health_check`, which is what the boot + periodic convergence checks write:

| Check | Last recorded | Verdict | Stale symbols |
| --- | --- | --- | --- |
| `sector_follow_cap5_vol` | 2026-09-17 16:30 IST | ok | `[]` |
| `scanner_universe_1m` | 2026-09-17 15:36 IST | ok | `[]` |
| `scanner_universe_D` | 2026-09-17 15:36 IST | ok | `[]` |
| `scanner_dry` | 2026-09-17 15:25 IST | ok | `[]` |
| `scanner_smoke_check` | 2026-09-17 09:18 IST | ok | `[]` |

**Verdict: clean.** Every universe reported fresh with zero stale symbols at
yesterday's post-close convergence. Today's boot-convergence row has not landed yet
(first scheduled jobs fire 09:00 IST), so this is yesterday's verdict carried
forward — the 09:10 arm and 09:33 smoke check are the live confirmations.

### Scheduler / overrides

**Scheduler: healthy.** Every `job_run` row for 2026-09-16 and 2026-09-17 is
`status='ok'` — no `error`, no `missed`. Confirmed registered and firing on both
days (IST):

- sector_follow: `daily_reset` 09:00 · `preentry_refresh` 15:02 · `smoke_check`
  15:03 · `entry` 15:05 · `exit` 15:10 · `eod_summary` 15:30 · `data_health` 16:30
- `scanner_comparison_eod` 15:45 ✅
- open15: `arm` 09:10 · `first_candles` 09:16 · `entry_verify` (28 fires/window) ·
  `exit` 09:30 · `exit_retry` 09:32 · `summary` 09:35
- futures_follow: `daily_reset` / `smoke_check` 15:18 / `entry` 15:20 / `exit` 15:25
  / `eod_watchdog` 15:28 / `eod_summary` 15:30
- also ok: `intraday_pullback_*`, `postmarket_review` 17:15, `option_liquidity_eod`,
  `multi_account_fill_reconcile` 09:40, `multi_account_eod_summary` 15:35,
  `trading_day_funnel`, `scanner_history_refresh`, `sector_follow_index_backfill`

**Active overrides: NONE.** `strategy_runtime_override` holds 3 rows, all with
`expires_at = 2026-08-12 10:00` — long expired. No pause, no kill-switch in force.

**Modes:** `open15_vol_breakout` = **LIVE** (real money). `simplified_engine`,
`sector_follow_cap5_vol`, `futures_follow_cap50` = sandbox.

**Broker session:** primary `dheeraj.sonawane` token stamped **2026-09-18 08:47 IST**,
not revoked — the headless auto-login (`AUTO_LOGIN_EARLIEST_TIME` 07:30) has already
run this morning. Master auto-login switch is ON.

### ⚠ Two things worth a look before 09:10

**1. All three child accounts are DISABLED.** The multi-account master switch is ON
(`multi_account_settings.enabled=1`) and all three children have an
`account_strategies` row for `open15_vol_breakout` (₹30k / ₹60k / ₹60k per trade),
but every `broker_accounts.is_enabled = 0`:

| # | Account | Client | Capital | Enabled | Auto | Last login |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | Swapna-zerodha | BC6810 | ₹96,000 | **no** | off | 2026-09-05 |
| 2 | Mai-Zerodha | QU0963 | ₹180,000 | **no** | off | 2026-09-05 |
| 3 | Didi-zerodha | JX8380 | ₹90,000 | **no** | off | 2026-09-11 |

Net effect: **no mirroring today.** If that is intentional, nothing to do. If you
want children on today's open15 trades, they each need enabling *and* a manual Kite
login on `/accounts` before 09:15.

**2. `open15_config` was edited at 08:51 IST today** (`updated_by='ui'`) — 19 minutes
before the arm. Effective settings for today's 09:10 arm:

- instrument `atm_option` · `max_trades` 3 · ₹60,000/slot · residual sizing ON
  (reserve 3%, min 2 lots)
- window: entry cutoff **09:24**, exit **09:30**
- `trade_side` = **both** · shadow-excluded-side ON, shadow cap 3
- **`trade_grades` = `A` only** — every B and C trigger becomes a full-slot
  *shadow/paper* row, no real order. This is the #726 pre-registered experiment;
  expect few or zero real fills on a day with no A-grade trigger (A needs trigger
  ≤ 09:22 AND vol ratio < 1.55× AND universe median > −0.30%).
- `vol_mult` 1.5 with **ceiling `max_vol_ratio` 1.8** (#721)
- profit lock ON: target ₹6,500, trail giveback ₹2,000, confirm 2 polls, poll 2s
- stop loss ON: ₹4,000 (long ₹4,000 / short ₹4,000)
- rolling watch-list ON, cadence 15s, top 3
- option liquidity percentile gate OFF · impact gate ON (max 2.0%) · min OI 500 lots
  · watched counterfactual ON

### Context — open15 real NET P&L, last 6 sessions

| Date | Real fills | Net |
| --- | --- | --- |
| 2026-09-17 | 2 | **+₹1,798** |
| 2026-09-16 | 2 | −₹5,222 |
| 2026-09-11 | 2 | −₹7,454 |
| 2026-09-10 | 3 | −₹7,524 |
| 2026-09-09 | 2 | +₹4,042 |
| 2026-09-08 | 3 | −₹1,466 |
| **6-session total** | **14** | **−₹15,826** |

Yesterday also carried 3 shadow rows (+₹7,838 gross), 1 sim (+₹1,654) and 10 watched
counterfactuals — the A-only grade filter now live is presumably the response to that
run. Worth checking the STOP-LOSS SCORECARD and by-grade breakdown on
`/open15_vol_breakout/logs` once today's summary lands at 09:35.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** — token already auto-minted at 08:47 IST
  today, so this looks covered. Kite allows one session per user: a manual web/app
  login will kill the API token.
- Today: **open 09:15 / close 15:30**
- **sector_follow entry 15:05 / exit 15:10** — not 15:20. Moved by #512 (NSE Closing
  Auction Session ends continuous trading in F&O cash scrips at 15:15) and
  hard-clamped to 15:10.
- open15 live window **09:15 → 09:30** (entry cutoff 09:24) — the only real-money
  strategy today.
- futures_follow 15:20 entry / 15:25 exit / 15:28 watchdog (sandbox).

---

*Standup produced read-only. No commits, no git operations, no DB writes.*

(Dheeraj — add EOD notes below in your own time.)
