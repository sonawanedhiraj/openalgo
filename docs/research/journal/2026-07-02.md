# 2026-07-02 Trading Journal

## 08:45 IST standup

### Yesterday's deltas (2026-07-01)
23 commits. Highlights:

- **Scanner:** SMA(200) daily-depth warm-up surfaced + `_CALENDAR_BUFFER` widened 1.4→1.6 (#280/#281); Path B engaged for live ts-column 5m frame + divergence block (#278); NSE sectoral indices routed to NSE_INDEX via master contract (#259); orphan code-backed scanner deletion in /scanner UI (#264); don't evaluate replayed/seeded bars (#257).
- **Engine:** simplified_engine now honors persistent `strategy_mode` DB row (#162 Phase 2, #287); abandoned-exit P&L recovered from sandbox square-off fills (#285); position reconciliation runs in sandbox AND live, mode-aware (#273); live broker-position reconciliation at exit (#268); preflight strategy_db probe import fix (#289).
- **sector_follow:** 15:18 smoke-check gated on mapped indices, not defensive backfill set (#283); pre-15:20 in-process data refresh (#291); futures_follow_cap50 got its own 15:18 smoke check (#294).
- **Infra:** in-process `claude -p` veto, bridge dependency retired (#271); httpx shared HTTP/2 client `send()` serialized to fix hpack deque race (#269); telegram single-poller guard — inbound defers to UI bot (#251); `flip_mode` made the only strategy_mode writer (#261); dashboard data-freshness tile (#295); notifications fail-open for unknown event_types (#293).

Flag for awareness: one `fix(test):` (9300c2447 — pin EOD-reconciliation tests to IST day). No `WIP`, no `refactor:` commits.

### Working tree state
8 untracked files, 0 modified tracked files:
- `docs/research/journal/` — prior standup/journal notes (2026-06-29, 2026-07-01, 2026-07-02_morning_status)
- 3 rotated `log/openalgo_*.log` files

Nothing important uncommitted. Note: a stale `.git/index.lock` was present and could not be removed (permission) — benign for this read-only run, but if git ops fail later, clear it.

### Pre-market freshness
⚠️ Overnight convergence check (03:11 IST today) reported **all 216 scanner-universe symbols stale** on both `1m` and `D` intervals (`overall_ok=0`, `skipped_fresh_count=0`). This is the expected pre-login state — the daily ~3 AM Zerodha token expiry blocks the historical fetch, so the 03:11 refresh attempt cannot complete. It should self-heal once you re-login and the boot/periodic backfill runs. **Verify the scanner universe clears after login** before relying on the in-house scanner today.

- `sector_follow_cap5_vol` freshness last checked 2026-07-01 11:00 (ok=1) — no fresh check yet today.

### Scheduler / overrides
- **No active runtime overrides.** The only `strategy_runtime_override` row (a sector_follow pause) expired 2026-07-01 10:00 — not in effect today.
- **Modes:** `sector_follow_cap5_vol` = **LIVE**, `futures_follow_cap50` = sandbox, `simplified_engine` = sandbox.
- sector_follow + scanner_comparison_eod jobs are registered at boot per code; live APScheduler verification isn't possible from this read-only sandbox (no localhost access) — confirm on the running app if in doubt.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily 3 AM token expiry) — **extra important today**, the overnight backfill found the scanner universe stale.
- Today: open 09:15 / close 15:30 / sector_follow entry 15:20.

(Dheeraj — add EOD notes below in your own time.)
