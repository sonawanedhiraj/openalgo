# 2026-08-21 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
- **0 commits on 2026-08-20 (Thu).** Last activity was Wed 2026-08-19, 5 commits — all open15/scanner hardening:
  - `8fd5e8cda` [#648] feat(scanner): split the universe into watched vs tradeable
  - `d4dc837de` [#647] fix(open15): not in F&O = not watched — a fact, not a gate
  - `d444a8726` [#645] fix(open15): "effective today" describes the ARM, not the process boot
  - `9a8438140` [#643] fix(open15): a raise in `_enter` erased the trigger; spend the residual cash
  - `50351a601` [#641] fix(open15): fill-true P&L at exit time, weighted-average fill price
- Flags for awareness: four of five are `fix(...)` on **open15_vol_breakout — the one strategy running LIVE**. All merged via PR into `dev`; no `WIP` and no `fix(test):` in the window.

### Working tree state
- Branch `dev`. **2 tracked files modified**, **86 untracked**.
  - Modified: `.gitignore`, `strategies/simplified_engine/LEARNINGS.md`
  - Untracked: overwhelmingly backtest scratch (`backtest/options_open15/*`, `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/inhouse_scanner/`, `backtest/news_event_study/*`), plus `.claude/launch.json` and an `audit/open15_replay_removal_backup_*.json`.
  - Nothing uncommitted looks load-bearing for today's session, but `strategies/simplified_engine/LEARNINGS.md` is a canonically-tracked file — worth committing when convenient.
- ⚠️ **`.git/index.lock` exists (0 bytes, 08:24 IST) and this task could not remove it** (sandbox mount denied the unlink). A stale `index.lock` will make the next `git add` / `git commit` fail with *"Unable to create '.git/index.lock': File exists"*. **Delete it manually on Windows before committing today.**

### Pre-market freshness
- **All green.** Latest `data_health_check` rows (written 2026-08-20 16:30 IST by the convergence checks that read `historify.duckdb`):
  - `sector_follow_cap5_vol` — ok, stale symbols `[]`
  - `scanner_universe_1m` — ok, stale symbols `[]`
  - `scanner_universe_D` — ok, stale symbols `[]`
  - `scanner_smoke_check`, `scanner_dry` — ok
- *Method note:* `historify.duckdb` could not be queried directly from this task's sandbox (no `duckdb` module and no package index; the project venv is Windows-side and not executable from Linux). The `data_health_check` table is the record written by the same freshness reads, so it is used as the authoritative proxy. `historify.duckdb.wal` is being written as of 08:25 IST, so the app is alive.

### Scheduler / overrides
- **Scheduler: clean.** Every job on 2026-08-20 recorded `status='ok'` in `job_run` — zero `error`, zero `missed`. Confirmed armed and firing: `sector_follow_smoke_check` / `_preentry_refresh` / `_entry` / `_exit` / `_eod_summary` / `_data_health` / `_index_backfill`, and `scanner_comparison_eod`. Also clean: all `futures_follow_*`, `open15_*`, `intraday_pullback_*`, `multi_account_*`, `postmarket_review`.
- No `job_run` rows yet for today — expected; the first daily fires are 09:00 IST.
- **Active runtime overrides: NONE.** The only three rows on record are the 2026-08-12 smoke-check pauses (sector_follow, futures_follow, intraday_pullback), all expired 2026-08-12 10:00. Nothing is holding entries today.
- **Modes:** `open15_vol_breakout` = **LIVE (real money)**; `simplified_engine`, `sector_follow_cap5_vol`, `futures_follow_cap50` = sandbox.

### Needs attention before open
1. 🔴 **P0 — `futures_follow_cap50` stranded carry.** Yesterday's post-market review fired `t1_exit_for_carry`: **1 NIFTY lot still open, oldest entry 2026-07-30 — 21 days old — with 0 exits.** This is the #497 shape on a T+1 strategy; it is sandbox, so it is not real money, but the lot has never been squared off. The contract has been failing repeatedly, not just yesterday.
2. 🟠 **In-house scanner vs Chartink recall = 0 for BUY.** `scanner_comparison` 2026-08-20: Chartink 17 BUY hits, in-house **0**, intersection 0, Jaccard 0.0. Per the documented caveat, a fully-disjoint result usually means tick starvation rather than a threshold mismatch. SELL side was parity (0 hits both sides).
3. 🟢 **open15 (LIVE) 2026-08-20 result** — 2 real fills, 1 broker rejection:
   - PFC (short): net **+₹17,911.93** (gross ₹18,564.00 − ₹652.07 modelled charges)
   - HINDALCO (short): net **−₹5,328.79** (gross −₹4,760.00 − ₹568.79)
   - **Net real P&L: +₹12,583.14**
   - BANDHANBNK (long): `rejected` → `fill='paper'`, net +₹16,886.96 — **paper, not money**; excluded from the real total per the #548 bucket rule.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily ~3 AM token expiry)
- Today: open **09:15** / close **15:30** / sector_follow entry **15:20** (note: `CLAUDE.md` records the post-CAS timing as refresh 15:02 → smoke 15:03 → entry 15:05 → exit 15:10, hard-clamped to 15:10; yesterday's `job_run` shows `sector_follow_entry` at 15:05 IST, so the CAS timing is what is live)
- open15 is the only LIVE strategy — arm happens 09:10, entry window to 09:29, exit 09:30. Boot OpenAlgo before 09:15 or the day is skipped.

*(Read-only standup — no commits, no git ops, no DB writes.)*

(Dheeraj — add EOD notes below in your own time.)

---

### 08:52 IST standup re-verification (second fire of `weekday-trading-standup`)

The scheduled task fired again after the 08:28 write above. Every fact was
**re-queried independently** and **nothing changed** — the standup above stands as written:

| Check | Re-verified at 08:52 IST | Verdict |
| --- | --- | --- |
| Commits in last 24h | 0 (last commit `8fd5e8cda`, Wed 2026-08-19 17:28) | unchanged |
| Working tree | branch `dev`, 2 tracked mods (`.gitignore`, `strategies/simplified_engine/LEARNINGS.md`), 88 untracked | unchanged |
| `data_health_check` | `sector_follow_cap5_vol`, `scanner_universe_1m`, `scanner_universe_D`, `scanner_dry`, `scanner_smoke_check` — all `overall_ok=1`, `stale_symbols=[]` | green |
| `job_run` 2026-08-20 | 216 fires, **all `ok`** — 0 `error`, 0 `missed` (208 on 08-19, 201 on 08-18) | green |
| `job_run` today | no rows yet — expected, first daily fires are 09:00 IST | normal |
| `strategy_runtime_override` | 3 rows, all `expires_at=2026-08-12 10:00` — **none active** | clear |
| `strategy_mode` | `open15_vol_breakout=live`; `simplified_engine`/`sector_follow_cap5_vol`/`futures_follow_cap50`=`sandbox` | unchanged |
| App liveness | `db/health.db` mtime 08:52, `db/openalgo.db` 08:33 — process is writing | alive |
| `postmarket_review` 2026-08-20 | 1 violation, `llm_status='ok'`: **P0 `futures_follow_cap50/t1_exit_for_carry` — "1 lots open, oldest entry 2026-07-30 (21d), but 0 exits today"** | still open |
| `open15_trades` 2026-08-20 | PFC S closed real gross ₹18,564 / chg ₹652.07 · HINDALCO S closed real gross −₹4,760 / chg ₹568.79 · BANDHANBNK L **rejected → `fill='paper'`** gross ₹17,388 / chg ₹501.04 | net real **+₹12,583.14** |
| `scanner_comparison` 2026-08-20 | BUY: Chartink 17 / in-house 0 / intersection 0 / Jaccard 0.0 · SELL: parity (0 both sides) | 🟠 recall 0 |

**⚠️ Telegram delivery FAILED from this run.** The bot token decrypted correctly from
`bot_config` (`is_active=1`, chat `1345069591`), but the sandbox egress proxy refuses
`api.telegram.org` (`ProxyError: Tunnel connection failed: 403 Forbidden`). No message
was sent by this fire — **this journal file is the only delivery**. If the 08:28 fire
also failed silently, the Telegram arm of this task is currently non-functional from
the Cowork sandbox and needs a host-side send path.

*(Read-only: no commits, no `git add`, no DB writes. Journal file left as a working-tree modification.)*
