# 2026-08-31 Trading Journal

## 08:45 IST standup

> ⚠️ **This standup ran LATE — 09:38 IST, not 08:45.** Market was already open and
> the open15 window (09:15–09:30) had already completed by the time it executed.
> The section below is therefore a *post-open* state report, not a pre-open one.
> Worth checking why the scheduled task fired ~53 min late.

### Yesterday's deltas

**0 commits in the last 24h** — yesterday was Sunday. Last activity was Friday 2026-08-28.

Recent commits (Friday and back):

- `34052675c` (08-28) — Merge PR #689 from `fix/688-auto-login-notify-importerror`
- `e008a7dfb` (08-27) — `[#688] fix(notify)`: silently-dropped alerts — three sites imported a `notify` that never existed
- `c6bdd8b31` (08-27) — Merge PR #687 from `feat/686-per-mode-realized-metrics`
- `4d2fef1cd` (08-27) — `[#686] feat(strategies-ui)`: realized CAGR/Sharpe/MaxDD for open15, futures_follow, intraday_pullback
- `3f70434be` (08-26) — Merge PR #683 from `feat/682-open15-waiting-and-holiday-gate`
- `0aa9a7a15` (08-26) — `[#682] feat(open15)`: pre-open "waiting" feed state + holiday gate at the arm

No `WIP` / `fix(test):` / `refactor:` commits to flag. Branch: `dev`.

### Working tree state

**103 entries** in `git status --porcelain` — but only **2 are tracked modifications**:

- `M .gitignore`
- `M strategies/simplified_engine/LEARNINGS.md`

The other **100 are untracked** — almost entirely backtest scratch under
`backtest/options_open15/`, `backtest/open15_rolling/`, `backtest/open15_missed_days/`,
`backtest/inhouse_scanner/r60/`, plus `backtest/news_event_study/` scripts and an
`audit/open15_replay_removal_backup_20260817_165557.json`.

Nothing looks like an accidental in-flight source edit. The `LEARNINGS.md` change is
the one worth a glance before it drifts further.

### Pre-market freshness

⚠️ `db/historify.duckdb` could **not** be read directly from the sandbox — permission
denied, because the live OpenAlgo process holds the file. That is the expected/healthy
state, not a fault. Freshness was therefore read from the `data_health_check` table,
which is the system's own verdict derived from that same DuckDB:

| Check | Last run (IST) | Verdict | Stale symbols |
| --- | --- | --- | --- |
| `scanner_universe_1m` | 2026-08-31 09:26 | ✅ ok | none |
| `scanner_universe_D` | 2026-08-31 09:26 | ✅ ok | none |
| `scanner_smoke_check` | 2026-08-31 09:18 | ✅ ok | none |
| `scanner_dry` | 2026-08-31 09:35 | ✅ ok | none |
| `sector_follow_cap5_vol` | 2026-08-27 16:30 | ✅ ok | none |

**Verdict: feeds are fresh.** One caveat — `sector_follow_cap5_vol`'s 16:30 IST data
health job has **no row for Friday 2026-08-28**. Its last successful check is Thursday.
Not stale data as such (the scanner universes cover the overlapping symbols and are
green), but the sector_follow-specific check didn't run on Friday. See below.

### Scheduler / overrides

**Active overrides: NONE.** All three `strategy_runtime_override` rows expired
2026-08-12 10:00 (smoke-check auto-pauses from that day). Nothing is holding entries today.

**Strategy modes:**

| Strategy | Mode |
| --- | --- |
| `open15_vol_breakout` | 🔴 **live** (real money) |
| `sector_follow_cap5_vol` | sandbox |
| `futures_follow_cap50` | sandbox |
| `simplified_engine` | sandbox |

**Today's jobs — armed and firing.** 31 `job_run` rows today, all `ok`, none `missed`.
open15 armed at **09:13:51** (universe 207, mode live, `atm_option`, 3 slots ×
₹60,000, rolling watch-list on) and the full chain ran: first candles 09:16 →
entry verify each minute → exit 09:30 → retry 09:32 → summary 09:35.

⚠️ **Two scheduler observations worth following up:**

1. **App restarted at 09:13:42 IST today**, i.e. *after* the 09:10 arm slot. There is
   no `open15_arm` row in `job_run` for today — the arm happened via the boot-time
   catch-up path at 09:13:51 instead. It worked (09:13 < 09:15, so the day was not
   lost), but it was ~3 min of margin. The CLAUDE.md ops rule — *boot OpenAlgo before
   09:15 IST* — was met by a narrow margin, not comfortably.

2. **Friday 2026-08-28: three `sector_follow` jobs recorded `missed`** —
   `sector_follow_preentry_refresh` (15:02), `sector_follow_smoke_check` (15:03) and
   `sector_follow_entry` (15:05). The app was **not** restarted (booted 08:44, log runs
   continuously to 15:51), so these are genuine APScheduler misfires, not a bounce.
   `sector_follow_exit` (15:10) and `sector_follow_eod_summary` (15:30) both ran fine.
   Net effect: **sector_follow took no entries on Friday.** It is in sandbox so no money
   was at stake, but a silently-skipped entry window is exactly the failure shape the
   expectation contracts exist to catch. A plausible cause is shared-scheduler thread
   starvation — today's `scanner_preentry_refresh` ran for **623 seconds** (scheduled
   09:16, fired 09:26), which is long enough to starve the pool if it coincides with
   another window. Worth a `type:bug` issue.

### Today's open15 result (already complete — 09:15–09:30 window)

Selected 10 · entered 3 · **filled 2 (real)** · 1 paper (rejected) · 4 rolling additions.

| Symbol | Side | Contract | Fill | Gross | Charges | **Net** |
| --- | --- | --- | --- | --- | --- | --- |
| LTF | S | LTF29SEP26310PE | real | −₹3,150.00 | ₹417.74 | **−₹3,567.74** |
| HDFCBANK | L | HDFCBANK29SEP26740CE | real | −₹6,500.00 | ₹494.83 | **−₹6,994.83** |
| KAYNES | S | KAYNES29SEP263850PE | **paper** | +₹20,535.00 | ₹520.30 | *(+₹20,014.70 — not real)* |

**Real net today: −₹10,562.57.** (Friday 08-28 for contrast: real net **+₹11,705.07**
across WIPRO / TCS / INFY.) Fill reconciliation ran clean — 2 reconciled, 0 pending,
0 demoted, 0 unresolved.

🔴 **The KAYNES rejection is the item to look at.** It was rejected by Zerodha with
*"MIS LIMIT orders are blocked for this KAYNES contract due to its open interest (OI)
being less than 500 lots"* — the exact rule the #595 broker-OI watch-list filter exists
to mirror. KAYNES was a **rolling** watch-list addition, and the filter is supposed to
run on rolling additions too. It did not block this one. Two days running now that a
rolling addition has been broker-rejected (PREMIERENE on 08-28, also `rolling`, also
paper). Worth checking whether the OI verdict cache or the rolling path is bypassing
`production_oi_filter`. Note the paper row would have been the day's only winner at
+₹20k — so this is not a cosmetic gap.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~06:30–07:30 AM IST token flush;
  Kite also kills the API session on any separate web/app login).
- Today: market **open 09:15** / **close 15:30**.
- `sector_follow_cap5_vol` entry **15:05**, exit T+1 **15:10** (post-CAS timing, issue #512 —
  *not* the old 15:20/15:25).
- `futures_follow_cap50` entry **15:20**, exit T+1 **15:25**, watchdog 15:28.

---

*(Dheeraj — add EOD notes below in your own time.)*
