# 2026-09-02 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
6 commits (3 feature commits + 3 merges), all merged to dev via PR — no WIP, no `fix(test):`, no bare `refactor:`:

- `bfa910595` — Merge PR #695 (`feat/694-system-shutdown`)
- `cc037354e` — `feat(system)`: guarded shutdown button on `/dashboard` (#694)
- `543bec5ad` — Merge PR #693 (`feat/692-open15-pnl-curve`)
- `d835d426b` — `feat(open15)`: intra-hold P&L curve + live P&L on `/logs` (#692)
- `27bb7444a` — Merge PR #691 (`feat/690-child-residual-sizing`)
- `6771b01f4` — `[#690] feat(multi-account)`: parent's open15 residual-sizing flag extends to child mirrors

Nothing flagged for awareness — clean lifecycle discipline (every commit carries an issue number, every PR merged).

### Working tree state
84 entries in `git status --porcelain`, but only **2 are tracked modifications**:

- ` M .gitignore`
- ` M strategies/simplified_engine/LEARNINGS.md`

The other 82 are untracked research/backtest scratch (`backtest/options_open15/*`, `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/inhouse_scanner/r60/`, `backtest/news_event_study/*`, an `audit/open15_replay_removal_backup_*.json`, `.claude/launch.json`).

⚠️ `strategies/simplified_engine/LEARNINGS.md` is uncommitted — per CLAUDE.md, strategy learnings are supposed to go direct-to-dev. Worth committing at your discretion.

### Pre-market freshness
**Green.** Direct `historify.duckdb` read was not possible from the sandbox (file held by the running app / permission denied on the mount), so this reads the system's own verdict in `data_health_check`:

| Check | Last run (UTC) | Verdict |
| --- | --- | --- |
| `scanner_universe_1m` | 2026-09-01 14:49 | ok — 216 symbols fresh, 0 stale |
| `scanner_universe_D` | 2026-09-01 14:49 | ok — 216 symbols fresh, 0 stale |
| `sector_follow_cap5_vol` | 2026-09-01 11:00 | ok — all 8 indices + 30 stocks at `staleness_days: 0`, `last_date: 2026-09-01` |
| `scanner_smoke_check` | 2026-09-01 03:48 | ok |
| `scanner_dry` | 2026-09-01 09:55 | ok — last in-house hit 15:20 IST |

The post-close convergence loop ran three times (13:53 / 14:25 / 14:49 UTC) and each pass reported `found_stale: []`, `errors: []`. The morning passes did find stale symbols (09:58, 10:05 UTC) and refreshed them — normal broker-lag catch-up, fully converged by close.

### Scheduler / overrides

**Schedulers — all green yesterday.** Every job in the 2026-09-01 window fired with `status='ok'`, zero `error`/`missed` rows across the last 3 days. Key confirmations for today:

- `sector_follow_*` — full set armed and fired yesterday: `daily_reset` 09:00, `preentry_refresh` 15:02, `smoke_check` 15:03, `entry` 15:05, `exit` 15:10, `eod_summary` 15:30, `data_health` 16:30, `index_backfill` 16:05 (all IST).
- `scanner_comparison_eod` — fired 15:45 IST, ok.
- Also green: `open15_*` (arm/first_candles/entry_verify ×14/exit/exit_retry/summary), `futures_follow_*` (6 jobs), `intraday_pullback_*`, `multi_account_*`, `postmarket_review` (17:15), `option_liquidity_eod`, `trading_day_funnel`.

No `job_run` rows for 2026-09-02 yet — expected, the first fire is the 09:00 IST daily reset. The app is up and writing (`openalgo.db-wal` mtime 08:50).

**Overrides — none active.** Three `strategy_runtime_override` rows exist (`sector_follow_cap5_vol`, `futures_follow_cap50`, `intraday_pullback_top2`), all `pause` from the 2026-08-12 smoke-check failure, all with `expires_at = 2026-08-12 10:00` — **long expired, nothing is held today**.

**Strategy modes:**

| Strategy | Mode |
| --- | --- |
| `open15_vol_breakout` | **live** (real money) |
| `sector_follow_cap5_vol` | sandbox |
| `futures_follow_cap50` | sandbox |
| `simplified_engine` | sandbox |

### ⚠️ One P0 to look at — `futures_follow_cap50` carry contract

Yesterday's `postmarket_review` (2026-09-01) fired **1 violation, P0**:

```
futures_follow_cap50 / t1_exit_for_carry
"1 lots open, oldest entry 2026-08-28 (4d), but 0 exits today"
fingerprint 38bd638a20a9
```

This is the #497 detector shape. **On the evidence it looks like a stale journal residual rather than a live 4-day carry** — worth 5 minutes of your eyes, not an emergency:

- Recent cycles all net flat: 2 BUY lots on 08-26 → SELL 2 lots on 08-27 (`t+1_exit`); 2 BUY lots on 08-28 → SELL 2 lots on 08-31 (`t+1_exit`). Both round-tripped cleanly.
- Across the **whole** journal: 20 BUY lots vs 19 SELL lots = **net 1**. The imbalance predates the 08-20 window, so the contract's FIFO walk attributes the orphan to the newest unmatched entry date (08-28) — which is why the message reads as a fresh carry when the recent pairs are actually balanced.
- `futures_follow_exit` fired `ok` on 09-01, and 08-31's exit is journaled — the exit path is not dead.
- Mitigating: this strategy is in **sandbox** (virtual ₹1Cr book), so no real capital is exposed.

Suggested action: reconcile the one orphan lot in `futures_follow_trades` (or confirm it against the sandbox position book) so the contract stops firing P0 daily on the same fingerprint. Consider opening a `type:bug` issue if it's a real journal gap.

### Reminders
- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~06:30–07:30 token flush; Kite also kills the API session on any separate web/app login).
- Today: **open 09:15 / close 15:30**.
- `sector_follow_cap5_vol` entry **15:05**, exit **15:10** (moved from 15:20/15:25 on 2026-08-03 for the NSE Closing Auction Session — issue #512).
- `futures_follow_cap50` entry 15:20 / exit 15:25 / watchdog 15:28.
- `open15_vol_breakout` arms 09:10, entry window to 09:29, exit 09:30 — **and it is LIVE**.

(Dheeraj — add EOD notes below in your own time.)
