# 2026-07-24 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
- 3 commits:
  - `a3c2a2ba7` [#440] feat(mode): per-strategy UI-driven live/sandbox order routing (#441)
  - `5d3e7b67b` [#438] fix(orders): convert live stock-option MARKET/SL-M to protective LIMIT/SL (#439) — order-path behavior change, worth awareness
  - `6967207b3` [#435][#437] feat(open15): ATM option shadow columns + Stock/Option instrument toggle (#436)
- No `WIP` / `fix(test):` / `refactor:` commits to flag.

### Working tree state
- 37 entries, all **untracked** — 0 tracked-file modifications.
- Content is benign: journal `.md` files (07-08 → 07-24), new backtest dirs (`backtest/inhouse_scanner/`, `backtest/options_open15/`, `docs/research/strategy/monthly_options_buy/`), and two DB backups (`db/openalgo.db.bak.20260714_*`, `db/sandbox.db.bak.20260714_*`).
- Nothing important is uncommitted — no source-code changes sitting in the tree.

### Pre-market freshness
- **sector_follow_cap5_vol universe: FRESH** — last data_health check 2026-07-23 11:00, ok=1, 0 stale (38 symbols).
- **⚠️ scanner universe (1m AND D): STALE** — overnight boot check at 2026-07-24 03:18 reported ok=0 with **216/216 symbols stale**. `historify.duckdb` last written 2026-07-23 15:50.
  - Root cause is almost certainly the ~3 AM Zerodha token expiry: the 03:18 convergence ran after expiry with no live broker session, so it couldn't fetch and everything reads behind. This should self-heal once Dheeraj re-logs in and the boot/periodic backfill convergence runs (15:30–17:00 loop + straggler tick 09:20–15:30).
  - **Action:** after re-login, verify scanner 1m/D convergence catches up (check `/sector_follow_cap5_vol/api/data_health` or the scanner health rows) before relying on in-house scanner signals today.

### Scheduler / overrides
- **App is running** — `log/errors.jsonl` live-updating (last write 08:45). Jobs register at boot; the in-process APScheduler store can't be enumerated read-only, but the service is up.
- **Active runtime overrides: NONE** — both `strategy_runtime_override` rows are expired (sector_follow pause exp. 07-01; intraday_pullback_top2 pause exp. 07-21). No entries are being held today.
- **Broker session EXPIRED** — today's errors show Zerodha auth/token failures: "Incorrect api_key or access_token", margin auth errors, WebSocket 403 Forbidden, "Auth/token failure detected — will not retry. Refresh token". Expected post-3AM; **re-login clears it.**
- **Strategy modes:** `sector_follow_cap5_vol` = **LIVE**, `open15_vol_breakout` = **LIVE** (flipped 03:05 today via UI by dheeraj.sonawane); `simplified_engine` = sandbox, `futures_follow_cap50` = sandbox. Two strategies route **real orders** today.
- **Log noise (non-blocking):** "Failed to query strategy_llm_config" ×142 today — LLM-veto probe fails open, not a trade blocker; worth a look when convenient.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily 3 AM token expiry) — **broker session is currently expired.**
- Today: open 09:15 / close 15:30 / sector_follow entry 15:20.
- Two LIVE strategies (sector_follow_cap5_vol, open15_vol_breakout) — real orders will route.

(Dheeraj — add EOD notes below in your own time.)
