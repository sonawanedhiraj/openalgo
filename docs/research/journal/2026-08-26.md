# 2026-08-26 Trading Journal

## 08:45 IST standup

_(generated read-only by the scheduled `weekday-trading-standup` task — not committed)_

### Yesterday's deltas

7 commits on `dev` (4 substantive + 3 merges), all issue-linked:

- `57d4e1b8c` **[#675] fix(tick-liveness)** — auto-heal ladder now paces on **ticks**; the bar close stays the health verdict. Fixes the 2026-08-25 case where step 2 escalated at +120s and re-killed a feed step 1 had just healed.
- `f9b8c86c4` **[#677] feat(open15)** — feed-health indicator + **clock-based selection finalize**. Past 09:17 an armed-but-unfinalized day finalizes from the 09:16 quote snapshot, so a dead feed no longer silently produces zero seeds.
- `b10120bdd` / `15547ef46` **[#673] fix(ws-proxy)** — reconnect snapshot reads pooled-wrapper `subscriptions` and verifies against the client `subscription_index`; index restore is the PRIMARY path on login reconnects (WARNING, not a daily ERROR).
- Merges: `#679`, `#678`, `#674`.

Nothing tagged `WIP`. No `refactor:` commits. No `fix(test):` commits.

**Note:** today's 08:52 boot is the **first session running all three fixes.** Yesterday's live incident was a 16-minute tick-feed outage (08:57 boot → watchdog heal 09:31) that spanned open15's entire entry window — open15 is the only live-money strategy. Watch the 09:10 arm and the new feed chip on `/open15_vol_breakout/logs`.

### Working tree state

**95 entries** — 4 tracked modifications, 91 untracked. App logged `WARNING in app: Dirty working tree detected at boot` at 08:52:48.

Tracked:

| Status | Path |
| --- | --- |
| ` M` | `.gitignore` |
| ` M` | `strategies/simplified_engine/LEARNINGS.md` |
| `M ` | `test/integration/test_mid_session_moments.py` **(staged)** |
| `MM` | `test/test_scanner_observability_205.py` **(staged + unstaged)** |

⚠️ The two staged test files are plausibly residue from yesterday's pytest-on-the-live-host event (10:05:33 IST, flagged as P1 in the 08-25 post-market triage — test output wrote three fabricated `auto-heal EXHAUSTED` errors into the production log). Worth deciding whether to keep or discard before they drift further.

Untracked bulk is research scratch (`backtest/options_open15/`, `backtest/open15_rolling/`, `backtest/inhouse_scanner/r60/`) plus **8+ accumulated `db/openalgo.db.bak.*` snapshots** — candidates for cleanup.

### Pre-market freshness

**Verdict: OK.** No overnight failure.

Direct `historify.duckdb` query was **not possible from this sandbox** (no `duckdb` module, no network to install it, and the project `.venv` is Windows-only — `_duckdb.cp312-win_amd64.pyd`). Fell back to two independent read-only sources:

- **`data_health_check` (latest per strategy)** — all green, zero stale symbols:
  - `scanner_universe_1m` — ok, stale=0 (2026-08-25 19:46 IST)
  - `scanner_universe_D` — ok, stale=0 (2026-08-25 19:46 IST)
  - `sector_follow_cap5_vol` — ok, stale=0 (2026-08-25 16:30 IST)
  - `scanner_dry` / `scanner_smoke_check` — ok, stale=0
- **`db/historify.duckdb`** last written 2026-08-25 19:36 IST (4.3 GB).

**Benign noise to ignore:** the 08:54 boot convergence logged `sector_follow index catch-up: 8/8 (100%) indices still stale` and `stock catch-up: 30/30 (100%) stocks still stale`. This is the normal **pre-open artifact** — the staleness predicate expects bars through today, and at 08:54 today's 1m bars do not exist yet, so the catch-up job fetches nothing and verifies "still stale". Identical warnings appear at the same hour on 2026-08-25 and 2026-08-24, and both days ended with green health rows. The daily-`D` convergence was still running normally at 08:55 (upserting 2 records/symbol, `2026-08-24..2026-08-26`).

### Scheduler / overrides

**Scheduler: healthy.** All 38 job ids fired `ok` on 2026-08-25 — **zero errors, zero missed, since 2026-08-22**. Confirmed armed for today (jobs registered at the 08:52 boot):

- `sector_follow_*` — daily_reset / preentry_refresh / smoke_check / entry / exit / eod_summary / data_health / index_backfill, all `ok` yesterday.
- `scanner_comparison_eod` — `ok` yesterday at 15:45 IST.
- `open15_*`, `futures_follow_*`, `intraday_pullback_*`, `multi_account_*`, `postmarket_review` (17:15) — all `ok`.

**Active overrides: NONE.** The three rows in `strategy_runtime_override` all expired 2026-08-12. Nothing is paused.

**Modes:** `open15_vol_breakout` = **LIVE** · `sector_follow_cap5_vol` / `futures_follow_cap50` / `simplified_engine` = sandbox.

### ⚠️ Needs a look before 09:15

1. **Child-account tokens look dead.** At 08:54:20 the log shows `ERROR in order_api: Incorrect api_key or access_token` and `account_open15_service: child positions read failed (broker=zerodha)` ×2, with `403 Forbidden` on `https://api.kite.trade/portfolio/positions`. Child account 2 (`Mai-Zerodha`) *did* report `auto-login succeeded` at 08:53:56, so at least one other child is unauthenticated. **open15 is live and mirrors real orders to children** — check `/accounts` before the 09:10 arm.
2. **Primary session appears alive** (WS connected + authenticated 08:53:22; historical downloads succeeding at 08:55) — but confirm on `/broker` anyway.
3. **Recurring P0 — `futures_follow_cap50/t1_exit_for_carry`.** Fired on 12 of the last 15 reviewed sessions (08-05 → 08-25). Yesterday's triage judged it a **journal accounting defect, not a stranded position**: 1 phantom lot dated 2026-07-30 (26d), sandbox book flat, `lots` column non-conserved. It will fire every trading day until the walk or the journal is fixed — worth an issue so the contract stops crying wolf.
4. **`broker_auto_login_service: Failed to send auto-login summary notification`** (08:53:56) — the auto-login worked; the notification path did not.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** (daily token expiry) — primary looks connected, **children do not**.
- Today: **open 09:15 / close 15:30**.
- `sector_follow_cap5_vol` entry **15:05**, exit **15:10** (moved from 15:20/15:25 on 2026-08-03 for the NSE Closing Auction Session — hard-clamped to 15:10).
- `futures_follow_cap50` entry 15:20 / exit 15:25 / watchdog 15:28.
- `open15_vol_breakout` arm 09:10, entry window to 09:29, exit 09:30.

### Standup delivery note

**Telegram send failed** — this sandbox's egress blocks `api.telegram.org` (`Tunnel connection failed: 403 Forbidden`). Bot config decrypted fine (`is_active=1`, chat `1345069591`); the message was simply never sent. This journal file is the full record.

---

(Dheeraj — add EOD notes below in your own time.)
