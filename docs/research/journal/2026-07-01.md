# 2026-07-01 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
- 3 commits since yesterday:
  - `7a2d1e149` [#244] fix(scanner): retry api_key in pre-subscribe loop + subscribe to broker_session_refreshed
  - `23dd5b944` [#231] feat: runtime divergence WARNING + Telegram alert across 3 multi-source services (#233)
  - `e1551123c` [#229] ci: wire smoke-stack gate (add scanner_clone, doc required checks) (#232)
- Nothing labeled WIP / `fix(test):` / `refactor:`. #244 is a scanner robustness fix; #231 adds divergence alerting; #229 is CI plumbing.
- Current branch: `fix/244-presubscribe-loop-on-api-key`.

### Working tree state
- 4 untracked files, nothing important uncommitted:
  - `docs/research/journal/2026-06-29.md`
  - `docs/research/journal/2026-06-29_morning_status.md`
  - `log/openalgo_2026-06-26.log.2026-06-26`
  - `log/openalgo_2026-06-30.log.2026-07-01.md` (this file, once written)
- Note: a `.git/index.lock` was present and could not be removed from the read-only sandbox (`Operation not permitted`). Harmless to this read-only standup, but if git ops feel stuck on the live machine, remove a stale `.git/index.lock`.

### Pre-market freshness
- Last recorded `data_health_check` for `sector_follow_cap5_vol`: **30-Jun 11:00 IST → overall_ok=1**, all 8 indices + 30 universe stocks fresh, last_date 2026-06-30, staleness 0 days.
- `scanner_universe_1m` and `scanner_universe_D`: last green check 30-Jun 10:53 (216 symbols fresh, 0 stale). Earlier 30-Jun runs auto-refreshed a handful (COLPAL / CONCOR / GMRAIRPORT) then converged.
- **Caveat:** no post-close (30-Jun ~15:30) or 01-Jul boot health-check row exists in `db/openalgo.db`. duckdb is not installable in this sandbox (no egress), so MAX(timestamp) could not be independently verified. The 11:00 check only confirms data through mid-morning 30-Jun, not the full 30-Jun close. **Verify the boot/periodic backfill populated 30-Jun close bars before relying on signals today.**

### Scheduler / overrides
- **Active runtime overrides: NONE.** The only `strategy_runtime_override` row is an expired `pause` on sector_follow (smoke_check_failed, coverage 0/30, created 30-Jun 09:48, expired 30-Jun 10:00). Not active today.
- **Persistent modes (`strategy_mode`):** `sector_follow_cap5_vol = LIVE`, `futures_follow_cap50 = sandbox`, `simplified_engine = sandbox`.
  - ⚠ **sector_follow is LIVE** — the DB `strategy_mode` row (set 24-Jun by 'harness') overrides `.env`'s `SECTOR_FOLLOW_CAP5_VOL_MODE=sandbox` via the resolver fall-through. Confirm this is intended before the 15:20 entry.
- **App state needs verification.** `log/openalgo_2026-07-01.log` shows a boot at 00:09:53 IST followed immediately by `Cleaning up running strategies... Cleanup complete` at 00:10:14 — i.e. a restart/shutdown overnight, not a steady run. The 30-Jun log tail and `errors.jsonl` show repeated `telegram.error.Conflict: terminated by other getUpdates request` (two bot pollers on one token). **Confirm OpenAlgo is up and the scheduler (sector_follow + scanner_comparison_eod jobs) is armed before 09:15.**

### Telegram
- ⚠ **Standup Telegram send FAILED from the sandbox** — egress to `api.telegram.org` is blocked (cowork allowlist is anthropic-only; both direct Bot API and web_fetch returned 403/blocked). Bot config is valid (token decrypts, `is_active=1`, chat_id 1345069591). This journal file is the durable record for today; the phone alert did not go out.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily 3 AM token expiry).
- Today: open 09:15 / close 15:30 / sector_follow entry 15:20.

(Dheeraj — add EOD notes below in your own time.)

---

## 08:45 IST standup (refresh — 08:48, supersedes above)

_Second automated run today. The section above ran earlier off a stale git state (3 commits, `.git/index.lock`). This refresh has the full overnight delta and a more serious git finding._

### 🔴 CRITICAL — git repo HEAD is corrupted
- `.git/HEAD` contains `ref: refs/heads/dev\n` followed by **33 trailing NULL bytes** (file is 53 bytes; valid is 20). Git rejects it: `fatal: your current branch appears to be broken` / `Failed to resolve HEAD as a valid ref`. **All normal git ops are broken right now** — `git status`, `git log`, commit, branch all fail.
- The `dev` ref itself is intact (`.git/refs/heads/dev` → `17030539b`, object present), and history is readable by SHA. Only `HEAD` is damaged.
- This is a recurring pattern on this repo (note the `.git/HEAD.lock.bak.*`, `index.bak-corrupt-*`, `config.bak-corrupt-*` files — likely sync/crash-induced null-byte writes).
- **Safe one-line fix (run on the live machine):** `printf 'ref: refs/heads/dev\n' > .git/HEAD` then `git status` to confirm. (Read-only standup did NOT apply it — per audit policy.)
- Consequence for THIS standup: the working-tree modification count could **not** be measured (git can't diff against a broken HEAD — `git status --porcelain` bogusly reports all ~2281 tracked files as `A`). Fix HEAD, then re-check the tree.

### Yesterday's deltas (last ~24h, read via `dev` SHA)
- **8 commits** on `dev` since 2026-06-30 (5 more landed overnight vs the earlier run's 3):
  - `17030539b` [#256] fix(scanner): don't evaluate replayed/seeded bars; downgrade skip log (#257) — 07-01 08:28
  - `9300c2447` [#252] **fix(test):** pin EOD-reconciliation B4 tests to IST day, not date.today() (#255) — 07-01 00:32
  - `3c5050d3d` [#235] fix(strategies-dashboard): bridge simplified_engine folder ↔ journal name (#254)
  - `cdb2b241d` [#238] fix(telegram): single-poller guard — inbound defers to UI bot (#251)
  - `545f00c38` [#249] test: regression coverage for boot-before-login broker-WS feed bring-up (#250)
  - `0e5172d50` [#244] fix(scanner): retry api_key in pre-subscribe loop + subscribe to broker_session_refreshed (#247)
  - `23dd5b944` [#231] feat: runtime divergence WARNING + Telegram alert across 3 multi-source services (#233)
  - `e1551123c` [#229] ci: wire smoke-stack gate (add scanner_clone, doc required checks) (#232)
- **Flags:** one `fix(test):` (#255, IST-day pinning — test hygiene, no runtime change). No WIP, no refactor. Theme = scanner robustness (#256/#244), telegram single-poller (#238), boot-before-login WS coverage (#250).

### Pre-market freshness — ⚠ NEEDS ATTENTION
- Today's early `data_health_check` rows (**07-01 02:38 and 03:09 IST**) for `scanner_universe_1m` AND `scanner_universe_D` both show **overall_ok=0 with the ENTIRE ~220-symbol universe stale.**
- Most likely cause: these ran **inside the ~3 AM Zerodha token-expiry window** — historical fetch fails on an expired daily token, so the boot/periodic convergence couldn't refresh. `db/historify.duckdb` file mtime is **30-Jun 09:52** (not written yet today), consistent with no successful backfill since yesterday.
- Yesterday closed healthy: last green `sector_follow_cap5_vol` check 30-Jun 11:00 (overall_ok=1), scanner universes green 30-Jun 10:53.
- **Action:** after Zerodha re-login, confirm the boot/periodic backfill converges today's feed to green **before** relying on 15:20 signals. If it doesn't self-heal, run the CLI catch-up (`services.scanner_universe_backfill` / `services.sector_follow_*_backfill`). duckdb couldn't be independently queried here (no module + no egress).

### Scheduler / overrides
- **Active runtime overrides: NONE.** Only `strategy_runtime_override` row is an **expired** sector_follow pause (smoke_check_failed 0/30, created 30-Jun 09:48, expired 30-Jun 10:00). Nothing blocks entries today.
- **Persistent modes:** `sector_follow_cap5_vol = LIVE`, `futures_follow_cap50 = sandbox`, `simplified_engine = sandbox`.
  - ⚠ **sector_follow is LIVE** (DB `strategy_mode`, set 24-Jun by 'harness'). Confirm intended before 15:20.
- **Scheduler live-state NOT verifiable from the sandbox** (OpenAlgo runs on the Windows host; 127.0.0.1:5000 here is the sandbox's own localhost). Jobs are registered at boot per `app.py` (sector_follow entry/exit/reset/smoke, scanner_comparison_eod 15:45). **Confirm OpenAlgo is up and re-logged-in before 09:15.**

### Telegram
- ⚠ **Phone alert NOT sent.** Egress to `api.telegram.org` is blocked from this sandbox (proxy returns `403 Forbidden` on CONNECT; allowlist is anthropic-only). Bot config is valid (`is_active=1`, chat_id 1345069591). This journal file is today's durable record; no Telegram delivery is possible from the scheduled-task sandbox.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily 3 AM token expiry).
- Today: open 09:15 / close 15:30 / sector_follow entry 15:20.
- 🩹 Fix `.git/HEAD` (command above) before any git work today.
