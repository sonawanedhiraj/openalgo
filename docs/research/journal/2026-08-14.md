# 2026-08-14 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
Branch `dev`, 3 commits in the last 24h — all `open15_vol_breakout`:

- `5dfd1c4ba` **[#597] fix(open15)** — late-boot arm must not clobber a traded day's persisted `day_log` (#598)
- `7589601d7` **[#595] feat(open15)** — seed/rolling broker-OI (≥500 lots) watch-list filter (#596)
- `42db78217` **[#593] fix(open15)** — look up `broker_pnl` by contract symbol for option rows (#594)

Flag for awareness: two of three are `fix(...)` on the **one strategy running with
real money** (`open15_vol_breakout` mode = `live`). No `WIP`, no `refactor:`, no
`fix(test):` in the window. All three carry issue links and merged via PR.

### Working tree state
- **2 tracked files modified** (uncommitted):
  - `.gitignore`
  - `strategies/simplified_engine/LEARNINGS.md`
- **143 untracked** entries — overwhelmingly scratch/expected: `backtest/options_open15/*`,
  `backtest/open15_rolling/`, `backtest/inhouse_scanner/`, `backtest/news_event_study/*`,
  7× `db/openalgo.db.bak.*` + 2× `db/sandbox.db.bak.*`, and a long tail of
  `docs/research/journal/*.md`.
- Nothing looks accidentally uncommitted, but `strategies/simplified_engine/LEARNINGS.md`
  is a canonically-tracked file per CLAUDE.md — worth a look before it drifts further.
- Note: the DB `.bak` files are accumulating (9 of them, back to 2026-07-14). Housekeeping candidate.

### Pre-market freshness
**Verdict: fresh as of yesterday's close. No stale symbols.**

⚠️ Method caveat: `db/historify.duckdb` could **not** be read directly this run — the
sandbox has no `duckdb` module and no PyPI network access (proxy 403), so `uv run --with
duckdb` also failed. Freshness was read instead from the canonical `data_health_check`
table in `db/openalgo.db`, which is what the convergence loop itself writes:

| check | last run (IST) | overall_ok | stale symbols |
| --- | --- | --- | --- |
| `scanner_universe_1m` | 2026-08-13 17:14 | ✅ 1 | `[]` |
| `scanner_universe_D` | 2026-08-13 17:14 | ✅ 1 | `[]` |
| `sector_follow_cap5_vol` | 2026-08-13 16:30 | ✅ 1 | `[]` |

All three arms converged clean inside the 15:30–17:00 periodic window. `historify.duckdb`
mtime is 2026-08-13 15:44 IST, consistent with a post-close write and no overnight activity.

### Scheduler / overrides
**Scheduler: healthy.** Every catalogued job fired `status='ok'` on 2026-08-13 — 35
distinct job ids, zero `error`, zero `missed`. Specifically confirmed armed and green:

- `sector_follow_preentry_refresh` (15:02) → `sector_follow_smoke_check` (15:03) →
  `sector_follow_entry` (15:05) → `sector_follow_exit` (15:10) → `sector_follow_eod_summary` (15:30)
- `sector_follow_data_health` (16:30), `sector_follow_index_backfill` (16:05)
- `scanner_comparison_eod` (15:45) ✅
- `scanner_smoke_check` (09:18), `scanner_preentry_refresh` (09:16), `scanner_history_refresh`
- `futures_follow_*` full chain (reset / smoke 15:18 / entry 15:20 / exit 15:25 / watchdog 15:28 / summary 15:30)
- `open15_*` full chain (arm 09:10 / first_candles / exit / retry / summary)
- `postmarket_review` (17:15) ✅

**Today so far:** `multi_account_login_reminder_0900` fired ok at 09:01 IST → the app is
up and the scheduler is live this morning.

**Active overrides: NONE.** The three most recent `strategy_runtime_override` rows are all
from the 2026-08-12 smoke-check failures (`futures_follow_cap50`, `sector_follow_cap5_vol`,
`intraday_pullback_top2`) and all **expired 2026-08-12 10:00 IST**. Nothing is holding
entries today.

**Current strategy modes:**

| strategy | mode |
| --- | --- |
| `open15_vol_breakout` | 🔴 **live** (real money) |
| `futures_follow_cap50` | sandbox |
| `sector_follow_cap5_vol` | sandbox |
| `simplified_engine` | sandbox |

### ⚠️ Recurring P0 — futures_follow_cap50 stranded carry (3rd consecutive day)

`postmarket_review` has fired the same deterministic contract violation three days running,
identical fingerprint `38bd638a20a9`:

| date | violation |
| --- | --- |
| 2026-08-11 | P0 `futures_follow_cap50/t1_exit_for_carry` — 1 lot open, oldest entry 2026-07-30 (**12d**), 0 exits today |
| 2026-08-12 | same — (**13d**) |
| 2026-08-13 | same — (**14d**) |

This is the **#497 shape** on a strategy whose entire design is a T+1 overnight hold: one
NIFTY lot entered 2026-07-30 has never been exited, and the age counter is just incrementing.
It is the sandbox book (`mode: sandbox`), so no real capital is at risk — but NRML has no
MIS auto-square-off underneath it, and the contract is doing exactly the job it was built
for: catching the silent-non-exit class on day one instead of day four. Worth deciding
today whether to square it manually or open an issue.

(Read-only standup — no action taken.)

### Reminders
- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~3 AM IST token expiry). Child family
  accounts on `/accounts` each need their own login if multi-account mirroring is on.
- **Today: open 09:15 / close 15:30.**
- Strategy timings (corrected — the 15:20 sector_follow figure predates the 2026-08-03
  NSE Closing Auction change, issue #512):
  - `sector_follow_cap5_vol` — refresh 15:02 / smoke 15:03 / **entry 15:05** / exit 15:10
  - `futures_follow_cap50` — smoke 15:18 / **entry 15:20** / exit 15:25 / watchdog 15:28
  - `open15_vol_breakout` — arm 09:10, entry window to 09:29, exit 09:30
  - EOD: scanner comparison 15:45, sector_follow data health 16:30, postmarket review 17:15

---

(Dheeraj — add EOD notes below in your own time.)
