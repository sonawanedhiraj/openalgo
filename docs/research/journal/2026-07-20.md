# 2026-07-20 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
3 commits (since 2026-07-19):

- `d4fa3b0` `docs(open15)` — embed look-ahead entry-mistake diagram in R58 research doc
- `4425ba0` `[#425] feat(open15_vol_breakout)` — sandbox strategy: mid-bar volume-surge breakout (PR #426)
- `f42fa79` `docs(strategy)` — R58 corrected to **REJECT** (entry-price look-ahead); BS option pricing recalibrated (#424)

Flags for awareness: no `WIP` / `fix(test):` / `refactor:` commits. The R58
reversal (edge was look-ahead, not real) is the substantive item — #425 ships
as a *measurement*, not a validated edge, per the corrected registry entry.

### 🔴 Blocker — open15 breakout service is NOT running today

```
[08:23:55] ERROR in app: Failed to initialize Open15 breakout service: cannot pickle '_thread.lock' object
[08:29:38] ERROR in app: Failed to initialize Open15 breakout service: cannot pickle '_thread.lock' object
```

Reproduced on **both** boots this morning. The strategy that merged last night
(#425/#426) fails at `init` — so its 4 APScheduler jobs (arm 09:10 / exit 09:30 /
retry 09:32 / summary 09:35 IST) are **not registered**. No measurement data will
be collected today. Likely an APScheduler job being handed an object holding a
`threading.Lock` (the service's own ZMQ tick SUB / state) — the scheduler tries
to serialize the job argument. Everything else booted clean.

*Read-only task: not fixed, not committed, not filed.*

### Working tree state
30 entries (branch `dev`):

- ` M strategies/simplified_engine/LEARNINGS.md` ← only tracked-file modification
- 29 untracked: `backtest/inhouse_scanner/`, `backtest/options_open15/`,
  `backtest/news_event_study/*.py`, `docs/research/strategy/monthly_options_buy/`,
  `docs/research/strategy/screener/2026-07-09_r53_loss_month_filters.md`,
  ~13 accumulated `docs/research/journal/*.md` files (07-08 → 07-20), and two
  DB backups (`db/openalgo.db.bak.20260714_175722`, `db/sandbox.db.bak.20260714_175722`).

Worth a cleanup pass at some point — the journal files and research docs are
meant to be tracked, and the two `.bak` DB dumps (from 07-14) are large and
should probably be deleted or moved out of the repo.

### Pre-market data freshness

| Universe | Last check | Verdict |
| --- | --- | --- |
| `scanner_universe_1m` | 2026-07-20 08:42 IST | 216/216 stale |
| `scanner_universe_D` | 2026-07-20 08:42 IST | 216/216 stale |
| sector_follow indices | 2026-07-20 08:30 IST | 10/10 stale |
| sector_follow stocks | 2026-07-20 08:30 IST | 30/30 stale |
| `sector_follow_cap5_vol` (health row) | 2026-07-17 16:30 IST | ✅ ok |

**Read as benign pre-market, not a failure.** Every arm ran a *completed*
backfill job (216 success / 0 failed) and still reported 100% stale — the
freshness predicate measures against today's expected 15:30 close, which
naturally doesn't exist at 08:42 on a trading morning. The identical pattern
resolved to `ok=1` on Friday by 15:18 IST. The daily-D re-settle also ran clean
(`status=ok window=2026-07-16..2026-07-20 resettled=True errors=0`).

**Watch point:** if the 09:20–15:30 straggler re-check loop (every 15 min) is
still reporting a large stale set after 09:30, that *is* real and would trip the
09:18 smoke check into a post-hold. `errors.jsonl` has **0 entries since 07-17**,
so nothing else failed overnight.

### Scheduler / overrides

Armed and confirmed in today's boot log:

- ✅ `sector_follow` jobs registered (mode=**sandbox**, strategy_id=1)
- ✅ `futures_follow` jobs registered (mode=sandbox, entry_mode=legacy, strategy_id=2, smoke_check=True)
- ✅ `scanner_comparison` EOD job (15:45 IST mon-fri)
- ✅ `scanner_smoke_check` (09:18 IST, min_cov=0.50), `scanner_preentry_refresh` (09:16 IST)
- ✅ EOD watchdog (`trending_equity_intraday`, 15:14 IST)
- ✅ `scanner_history_refresh` (16:00 IST), news ingest (5-min), straggler re-check loop
- ❌ **open15 breakout — failed to initialize (see blocker above)**

**Active overrides: none.** The only `strategy_runtime_override` row is a
`sector_follow_cap5_vol` pause that expired 2026-07-01 10:00. Nothing is
holding entries today.

**Two discrepancies worth an eyeball (not acted on):**

1. `strategy_mode` DB row says `sector_follow_cap5_vol` = **live** (set
   2026-06-24 by `harness`), but the boot log registered it as **mode=sandbox**
   — an env flag (`SECTOR_FOLLOW_CAP5_VOL_MODE`) is presumably winning the
   resolution. Since sector_follow is the alpha primary, worth confirming which
   book it actually trades into today.
2. `futures_follow` Stage-1 LLM veto logged **enforcement mode=off**, whereas
   the documented B4 default for a sandbox strategy is *active/enforcing*. An
   explicit `VETO_LAYER_MODE=off` or a `strategy_llm_config` row is overriding.

Also of note: OpenAlgo restarted twice this morning (08:24 and 08:29).

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~3 AM token expiry) —
  ✅ *already done today*: master contract downloaded 08:25 IST, broker WS
  connected + authenticated (user=dheeraj.sonawane, broker=zerodha).
- Today: open **09:15** / close **15:30** / sector_follow entry **15:20**
  (futures_follow entry 15:20, exit T+1 15:25, watchdog 15:28).

(Dheeraj — add EOD notes below in your own time.)
