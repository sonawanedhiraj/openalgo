# 2026-08-28 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
4 commits in the last 24h (branch `dev`, clean history — no `WIP`, no `fix(test):`, no `refactor:`):

- `34052675c` — Merge PR #689 (`fix/688-auto-login-notify-importerror`)
- `e008a7dfb` — **[#688] fix(notify): silently-dropped alerts** — three sites imported a `notify` that never existed. Worth knowing: alert paths that were silently dead are now live, so expect Telegram volume to be *correct* rather than *quiet*.
- `c6bdd8b31` — Merge PR #687 (`feat/686-per-mode-realized-metrics`)
- `4d2fef1cd` — **[#686] feat(strategies-ui)**: realized CAGR / Sharpe / MaxDD for `open15`, `futures_follow` and `intraday_pullback` on the strategies dashboard.

Both merged to `dev` via PR with issue links — lifecycle clean.

### Working tree state
- **100 entries**: 2 tracked-modified, 98 untracked.
- Tracked modifications (uncommitted):
  - `M .gitignore`
  - `M strategies/simplified_engine/LEARNINGS.md` ← strategy learnings sitting uncommitted; per CLAUDE.md, strategy-doc updates go **direct to dev**. Worth flushing.
- The 98 untracked are backtest/research scratch (`backtest/options_open15/*`, `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/inhouse_scanner/r60/`, `backtest/news_event_study/*`, plus an audit backup JSON). Nothing load-bearing appears to be uncommitted source.

### Pre-market freshness
`db/historify.duckdb` could not be queried directly from this sandbox (no `duckdb` module, no network to install, and the project `.venv` is a Windows venv not runnable here). Fell back to the authoritative in-app verdicts in `data_health_check` (`db/openalgo.db`), which is what the freshness gates themselves read:

| Check | Last run (UTC) | IST | Verdict | Stale symbols |
| --- | --- | --- | --- | --- |
| `sector_follow_cap5_vol` | 2026-08-27 11:00 | 16:30 | **ok** | none |
| `scanner_universe_1m` | 2026-08-27 10:15 | 15:45 | **ok** | none |
| `scanner_universe_D` | 2026-08-27 10:15 | 15:45 | **ok** | none |
| `scanner_smoke_check` | 2026-08-27 03:48 | 09:18 | **ok** | none |
| `scanner_dry` | 2026-08-27 09:55 | 15:25 | **ok** | none |

**Verdict: clean.** All 38 sector_follow symbols (8 indices + 30 stocks) and the full scanner universe reported fresh at yesterday's post-close convergence, zero stale. `historify.duckdb.wal` is being written as of 08:49 IST, so the app is live and the boot/periodic convergence is active. Caveat: this is yesterday's post-close verdict — today's 09:18 scanner smoke check is the first fresh read.

### Scheduler / overrides

**Job health — yesterday (2026-08-27): 216 fires, 0 errors, 0 missed.** Same for 08-26 and 08-25. All armed jobs confirmed present:

- `sector_follow_*` — daily_reset 09:00 ✓, preentry_refresh 15:02 ✓, smoke_check 15:03 ✓, entry 15:05 ✓, exit 15:10 ✓, eod_summary 15:30 ✓, data_health 16:30 ✓, index_backfill ✓
- `scanner_comparison_eod` — fired 15:45 ✓
- `open15_*` — arm 09:10 ✓, first_candles 09:16 ✓, entry_verify ×14 ✓, exit 09:30 ✓, exit_retry 09:32 ✓, summary 09:35 ✓
- `futures_follow_*` — all 6 jobs ✓ · `intraday_pullback_*` — all ✓ (eval ×84)
- `postmarket_review` — fired 17:20, **0 violations**, `llm_status=ok`, `sources_failed=[]`

Today (2026-08-28) has no fires yet at 08:45 IST — expected; first job is the 09:00 daily reset.

**Active overrides: NONE.** The three rows in `strategy_runtime_override` all expired 2026-08-12 10:00. Nothing is paused or kill-switched.

**Strategy modes:**

| Strategy | Mode |
| --- | --- |
| `open15_vol_breakout` | **LIVE** ⚠ (real money, arms 09:10, entry window to 09:29, exit 09:30) |
| `sector_follow_cap5_vol` | sandbox |
| `futures_follow_cap50` | sandbox |
| `simplified_engine` | sandbox |

open15 effective config: `atm_option`, 3 slots × ₹60,000, vol_mult 1.5, both sides, rolling watch-list ON (30s), shadow ON (3), OI floor 500 lots, residual sizing ON.

### Yesterday's open15 result (2026-08-27)
- **Real: 3 fills, net +₹11,510.69**
- Sim (never placed — slot cap / affordability): 1 row at +₹17,377. The single biggest move of the day was a slot we did not have. Worth a look at whether the funds clamp or `max_trades` was the binding constraint.

### Reminders
- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~06:30–07:30 token flush; also killed mid-session by any separate Kite web/app login). All 5 auth rows present and un-revoked, but presence ≠ a live token.
- ⚠ **`open15_vol_breakout` is LIVE** — it arms at 09:10 and can place real orders from 09:16. If the broker session is not up by 09:10, the day is skipped loudly.
- Today: **open 09:15 / close 15:30**. open15 entry window 09:16–09:29, exit 09:30. sector_follow entry **15:05**, exit T+1 15:10 (moved from 15:20/15:25 on 2026-08-03 for the NSE Closing Auction Session — see #512). futures_follow entry 15:20 / exit 15:25.

---

*Generated read-only by the 08:45 IST scheduled standup. No commits, no git ops, no DB writes.*

(Dheeraj — add EOD notes below in your own time.)
