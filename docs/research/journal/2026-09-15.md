# 2026-09-15 Trading Journal

## 08:45 IST standup

> ⚠️ **Degraded run — 4th trading day in a row.** The sandboxed Linux shell failed to
> mount again (`Plan9 share "c" not mounted`, identical error, 2 attempts — the
> 2026-09-08 Windows update is the stated cause). So **no `git`, no `duckdb`, no
> `python`, and no Telegram send.** Everything below is reconstructed read-only from
> `.git/logs/HEAD` and `log/openalgo_2026-09-15.log`. An attempt to read the live
> API (`/mode/status`, `/open15_vol_breakout/api/*`) via the browser was **refused —
> no logged-in session in that Chrome profile**, and logging in is out of scope for
> an automated run. See "Gaps".

---

### ✅ GOOD NEWS — OpenAlgo is UP and the auto-login worked

Boot at **08:50:54**, primary Zerodha **auto-login succeeded at 08:51:36** (fully
headless — no live session at boot → `broker auto-login: no live session at boot —
attempting auto-login` → token written 08:51:27 → `Primary Zerodha account auto-login
succeeded`). WebSocket reconnected immediately after (the 403s at 08:51:05/08:51:21
are the *pre-login* stale token, then `✅ WebSocket connected` at 08:51:36 and
subscriptions started flowing).

Master contract re-downloaded (last was 09-11): **110,315 records in 28 s**, cache
loaded in 6.29 s, catch-up tasks done.

**Boot was ~08:51, convergence started 08:51:29.** Friday's convergence took ~13 min;
if today's runs similar it lands ~09:04, i.e. **before the 09:10 arm**. Tight but OK.

### 🔴 Monday 2026-09-14 — OpenAlgo never ran at all

There is **no `log/openalgo_2026-09-14.log`**. The app was down for the whole of
Monday's session. Consequences:

- open15 did not arm, did not trade. No 09-14 row anywhere.
- sector_follow 15:05/15:10, futures_follow 15:20/15:25, `scanner_comparison_eod`
  15:45, option-liquidity sweep 15:45, data-health 16:30, `postmarket_review` 17:15 —
  **none fired**. This is now the **4th–5th consecutive session** of the missed-evening-
  jobs pattern (09-10, 09-11, 09-12, 09-14).
- APScheduler logged the whole missed set at boot (`was missed by 23:50:59`,
  `17:20:59`, …) — that block in today's log is the receipt.

⚠️ **The boot catch-up wrote a prior-day P&L snapshot for `dheeraj.sonawane` of
`realized=-64,675.58`** (08:52:12). No strategy traded Monday, so this is **not** a
strategy result — it is either manual activity or a carried position settling.
**Worth 30 seconds of your attention before the open.**

### Yesterday's deltas — 3 commits, and #726 is MERGED and LIVE today

| When (IST) | Ref | What |
| --- | --- | --- |
| Mon 23:09:32 | `fcad3b3` on `dev` | `docs(strategy-registry)`: round 63 — open15 winner patterns + A/B/C trade rating (#725) |
| Mon 23:30:13 | `e1ec13f` on `dev` | `docs(open15)`: trade-rating production plan — live watch-list grades, grade filter, paper for excluded grades (#726) |
| Tue 04:13:10 | — | checkout `dev` → `feat/726-open15-trade-rating` |
| Tue 04:24:57 | `4b6af76` on branch | `[#726] feat(open15)`: A/B/C trade rating at the trigger, live watch-list grades, `trade_grades` filter with paper for the rest |
| Tue 08:49:14 | `dev` **`e1ec13f` → `088dd67`** | `pull: Fast-forward` — **#726 merged into dev**, 2 min before boot |

**Flag for awareness:** no `WIP`, no `fix(test):`, no `refactor:`. But note the shape —
a feature was written at **04:25** and merged at **08:49**, and the app was booted at
**08:50:54 on the merged code**. The 04:22–04:25 block in today's log is the #726
pytest run (synthetic `AAA`/`CCC` symbols, `rating` and `vol_ratio_cap` events firing
as designed), so it was tested, but **today is #726's first live session with ~20
minutes between merge and arm.**

**#726 schema migration ran clean at boot** (08:50:54):
`open15_trades` gained `rating`, `rating_univ_median_pct`, `rating_vol_ratio`,
`rating_provisional_at_add`; `open15_config` gained `trade_grades`.

⚠️ **Unverified:** what `trade_grades` actually resolves to for today's arm. The env
seed default is `ABC` (= byte-identical to pre-#726 behaviour), but the column is new
and NULL, so it falls to the seed. **Glance at the `/logs` effective-config line after
09:10** to confirm the grade checkboxes read the way you want them — the decision rule
is pre-registered at ~40 new real fills, and today is fill #1 of that count.

### Working tree state — 106 entries, unchanged in substance from Friday

Source: `app.py` boot dirty-check at **08:50:54** (post-merge).

| Status | Path | Note |
| --- | --- | --- |
| `M ` | `.gitignore` | **Still staged in the index** — 4th standup running. Interrupted intent. |
| ` M` | `strategies/simplified_engine/LEARNINGS.md` | Unstaged edit |

The other ~104 are untracked scratch/output, unchanged in character: ~20
`backtest/options_open15/*.py` + `open15_rolling/`, `open15_missed_days/`,
`inhouse_scanner/r60/`, `news_event_study/`; **13 × `db/*.db.bak.*`** back to
2026-07-14 (still worth pruning); ~34 × `docs/research/journal/*.md` (2026-08-21 →
today, uncommitted per the audit-trail policy); 24 × `log/restart_*.{out,err}`;
new since Friday: `.claude/launch.json`, `db/.fuse_hidden0000003400000001`,
`url_1345069591.txt`. **No lost work.**

### Pre-market data freshness — ⚠️ genuinely behind, healing right now

❌ Could not query `db/historify.duckdb` directly (no shell). Read from the live
convergence log instead, which is better evidence anyway:

- **1m is behind across the board.** `aggregator_seeder` reports the same line for
  essentially every symbol: *"historify had 112 bars (min_required=166,
  today_ok=True), broker fallback returned 500 and wins"*. 112 < 166 means Friday's
  post-close 1m backfill never ran (app died 09:32) **and** Monday's never ran at all.
  The seeder is falling back to ~225 per-symbol broker calls — slower, but it **does**
  seed correctly.
- **Daily-`D` resettle is running now** — `Downloading <SYM>:NSE:D from 2026-09-10 to
  2026-09-15`, **2 records each** (= Fri 09-11 + Mon 09-14, exactly the two missing
  settled closes). This is #299 doing its job. Let it finish before 09:10.
- 🔴 **Sector indices: 8/8 still stale after a completed job** —
  `FINNIFTY, NIFTY, NIFTYAUTO, NIFTYFMCG, NIFTYIT, NIFTYMETAL, NIFTYPSUBANK,
  NIFTYPVTBANK` (08:51:34). Pre-open this is *partly* expected (it chases today's
  bars), but with two sessions of index 1m missing this is the input
  **`sector_follow_cap5_vol` needs at 15:05**. The 16:30 data-health job did not run
  Monday either, so nothing has auto-paused it. **Check
  `/sector_follow_cap5_vol/api/data_health` before 15:00.**
- ⚠️ **Option-liquidity coverage is now ~8 days stale** — the 15:45 EOD sweep last ran
  **2026-09-03** and has missed 09-10, 09-11, 09-12, 09-14. The #591 coverage ladder
  consumed by the 09:10 arm is **overstated**. The 1200 s convergence loop started at
  08:50:59 and may heal some of it. **Glance at the coverage card on
  `/open15_vol_breakout/logs` before 09:10.**
- One source-divergence WARNING: `INDUSTOWER` historify 378.45 vs broker 375.20
  (0.86% > 0.50% threshold). Below the #305 reference-check 1.0% reject line, so it
  will not block — but it is the shape that manufactures phantom gaps.

### Scheduler / overrides — all armed, verified from today's own boot

| Strategy | Jobs (IST) | Mode |
| --- | --- | --- |
| `open15_vol_breakout` | arm **09:10** / first-candles 09:16 / exit **09:30** / retry 09:32 / summary 09:35 / entry-verify every 1 min | 6 jobs registered ✅ |
| `sector_follow_cap5_vol` | refresh 15:02 / smoke 15:03 / entry **15:05** / exit **15:10** / reset 09:00 / EOD 15:30 | **sandbox**, `strategy_id=1` |
| `futures_follow_cap50` | smoke 15:18 / entry **15:20** / exit **15:25** / watchdog 15:28 / EOD 15:30 | **sandbox**, `entry_mode=legacy`, `strategy_id=2` |
| `intraday_pullback` | 5m eval / smoke 09:18 / flatten 15:15 / EOD 15:30 | **sandbox** |
| `scanner_comparison_eod` · open15 option-liquidity sweep | **15:45** mon-fri | registered ✅ |
| `postmarket_review` | **17:15** | registered ✅ |
| EOD watchdog | `trending_equity_intraday` **15:14** (declared 15:20) | 1 job, 0 skipped |

- Scanner: **216 watched, 212 tradeable** — 4 dropped `not_in_fno`
  (**DALBHARAT, EXIDEIND, NUVAMA, SAMMAANCAP**). `SCANNER_SYMBOLS` is still stale;
  #648/#647 working as designed. Aggregator universe 228 symbols, 5m interval.
- Scanner history provider warm-up: **212 symbols, 0 errors**.
- `futures_follow` Stage-1 LLM veto: enforcement **off**.
- Telegram **inbound** bot disabled (`TELEGRAM_INBOUND_ENABLED=false`) — expected.
- **Overrides: none surfaced.** `strategy_runtime_override` initialised with nothing
  logged as held. ⚠️ Could not query the table directly — this is absence of an alert,
  not a positive read.
- sector_follow rehydrate skipped 4 journal rows flat in the sandbox book
  (M&M, TATASTEEL, KOTAKBANK, VEDL) — correct #497 behaviour, not a problem.

### Reminders

- 🔑 **Zerodha re-login: already done for you.** The #654 watcher auto-logged-in at
  **08:51:36**. Verify on `/broker` if you want the belt-and-braces. Child accounts
  (`acct:*`) — **no child auto-login line in the log yet**; if you mirror today, check
  `/accounts`.
- Today: open **09:15** / close **15:30**.
- open15 window **09:16–09:24 entries**, exit **09:30** (arm **09:10**) ·
  sector_follow entry **15:05**, exit **15:10** · futures_follow entry **15:20**,
  exit **15:25**.
- ⚠️ The SKILL's stated "sector_follow entry 15:20" is **stale** — it moved to **15:05**
  on 2026-08-03 for the NSE closing-auction change (#512).

### Gaps in this run

1. **Shell mount failure four trading days running** (09-10, 09-11, 09-14, 09-15).
   The standup cannot run `git`, `duckdb`, or its own Telegram send. This needs a fix,
   not a workaround — it is now the default state, not an incident.
2. **No Telegram sent** — the token needs `database.telegram_db.get_bot_config()`
   decryption, which needs Python. **This file is the only delivery today.**
3. **Live API reads refused** — no authenticated browser session, so
   `strategy_runtime_override` rows, the open15 effective config (`trade_grades`!),
   and the option-liquidity coverage ladder are all *inferred from the boot log*,
   not read.
4. Today's data-freshness verdict is **mid-flight** — convergence was still running
   when this was written (08:52).

### Suggested first 20 minutes

1. **Watch convergence finish before 09:10** — daily-`D` resettle is filling Fri+Mon
   right now. If it is still grinding at 09:08, the arm reads partial data.
2. **Confirm `trade_grades` on the `/logs` effective-config line after the arm.**
   #726 merged 20 minutes before boot; today is its first live day and the grade
   filter is the one knob that can silently paper-trade a whole cohort.
3. **Check the option-liquidity coverage card** — the sweep last ran 09-03, so the
   #591 ladder is overstated going into the arm.
4. **Explain the −₹64,675.58 prior-day realized snapshot.** No strategy ran Monday.
5. **Fix the "app stops ~09:32 every day" problem.** Five sessions of evening jobs have
   now been lost, including every `postmarket_review` — the layer built to catch
   exactly this. This outranks any strategy tuning today.
6. Deal with the staged `.gitignore` (4th ask) and prune the 13 `db/*.db.bak.*`.

(Dheeraj — add EOD notes below in your own time.)
