# 2026-08-17 Trading Journal

## 08:45 IST standup

_Generated read-only by the scheduled `weekday-trading-standup` task. Not committed._

> **Telegram delivery FAILED.** `api.telegram.org` is not on the Cowork workspace egress allowlist
> (`cowork-egress-blocked`), and the sandbox VM's package proxy also refuses outbound HTTPS to it.
> The standup was **not** pushed to the bot — this file is the only copy. To restore Telegram
> delivery, add `api.telegram.org` under Settings → Capabilities → network allowlist.

### Yesterday's deltas

- **0 commits** in the last 24h (Fri 2026-08-14 and the weekend were quiet on `dev`).
- 8 commits in the last 7 days. Most recent three, for context:
  - `5dfd1c4ba` (08-13) `[#597] fix(open15): late-boot arm must not clobber a traded day's persisted day_log`
  - `7589601d7` (08-13) `[#595] feat(open15): seed/rolling broker-OI (>=500 lots) watch-list filter`
  - `42db78217` (08-13) `[#593] fix(open15): look up broker_pnl by contract symbol for option rows`
- No `WIP` / `fix(test):` / `refactor:` commits to flag.

### Working tree state

Branch `dev`. **147 entries** in `git status --porcelain`:

- **2 tracked files modified:**
  - `.gitignore`
  - `strategies/simplified_engine/LEARNINGS.md`
- **145 untracked** — nothing alarming, mostly scratch and backups:
  - `backtest/options_open15/` (~15 analysis scripts + parquet/json caches), `backtest/open15_rolling/`, `backtest/inhouse_scanner/`, `backtest/news_event_study/*`
  - 7× `db/openalgo.db.bak.*` and 2× `db/sandbox.db.bak.*` (dating back to 2026-07-14) — candidates for cleanup
  - a long run of `docs/research/journal/*.md` files from June onward that were never committed

The modified `strategies/simplified_engine/LEARNINGS.md` is the one worth a glance — per CLAUDE.md, strategy learnings go direct to `dev`.

### Pre-market freshness

**Verdict: clean as of Friday's post-close checks; nothing recorded yet today.**

Latest `data_health_check` rows (all `overall_ok=1`, empty stale-symbol lists):

| Universe | Last check (IST) | OK | Stale |
| --- | --- | --- | --- |
| `sector_follow_cap5_vol` | Fri 2026-08-14 16:30 | ✅ | 0 |
| `scanner_universe_1m` | Fri 2026-08-14 15:45 | ✅ | 0 |
| `scanner_universe_D` | Fri 2026-08-14 15:45 | ✅ | 0 |
| `scanner_dry` | Fri 2026-08-14 15:25 | ✅ | 0 |

No health row exists for today yet — expected, since the boot convergence check waits for a broker session and the Zerodha token expired at ~03:00 IST. `historify.duckdb.wal` is being written (09:02 IST), so the process is alive and will converge once you re-login.

**Caveat:** `historify.duckdb` could not be queried directly from this sandbox (no `duckdb` module and no package-index access; the project `.venv` is a Windows venv, unusable from Linux). The verdict above is derived from the `data_health_check` table in `db/openalgo.db`, which is the same signal the convergence jobs write. If you want a first-hand read, `GET /sector_follow_cap5_vol/api/data_health` on the running app.

### Scheduler / overrides

**Schedulers: healthy.** `job_run` shows **196 fires on Friday, 0 errors/missed**. Every relevant job landed at its armed time:

- `sector_follow_preentry_refresh` 15:02 · `sector_follow_smoke_check` 15:03 · `sector_follow_entry` 15:05 · `sector_follow_exit` 15:10 — all `ok`
- `futures_follow_smoke_check` 15:18 · `entry` 15:20 · `exit` 15:25 · `eod_watchdog` 15:28 — all `ok`
- `scanner_comparison_eod` 15:45 `ok` · `sector_follow_data_health` 16:30 `ok` · `postmarket_review` 17:21 `ok`

Today's 09:00 IST resets already fired `ok`: `sector_follow_daily_reset`, `futures_follow_daily_reset`, `intraday_pullback_daily_reset`, `scanner_dry_tripwire`, `intraday_pullback_eval`, `multi_account_login_reminder_0900`. Both jobs the standup is asked to confirm — sector_follow and `scanner_comparison_eod` — are armed.

> Note: the standup template still says "sector_follow entry 15:20". Per issue #512 (NSE Closing Auction Session, effective 2026-08-03) the chain moved to **refresh 15:02 → smoke 15:03 → entry 15:05 → exit 15:10**, hard-clamped to 15:10. The live `job_run` timings confirm the new window. Template is stale, the system is correct.

**Overrides: none active.** `strategy_runtime_override` holds 3 rows, all `pause`, all expired 2026-08-12 15:30 IST (the 08-12 smoke-check failures for `sector_follow_cap5_vol`, `futures_follow_cap50`, `intraday_pullback_top2`). Nothing is holding entries today.

**Current modes** (`strategy_mode`):

| Strategy | Mode |
| --- | --- |
| `open15_vol_breakout` | **live** (real money, since 2026-07-24) |
| `sector_follow_cap5_vol` | sandbox |
| `futures_follow_cap50` | sandbox |
| `simplified_engine` | sandbox |

### ⚠ Standing issue — futures_follow_cap50 T+1 exit

`postmarket_review` has fired the **same P0 contract violation three days running** (fingerprint `38bd638a20a9`):

> `futures_follow_cap50 / t1_exit_for_carry` — 1 lot open, oldest entry **2026-07-30**, **carry age 15 days**, 0 exits today, 0 entries today.

This is the #497 failure shape on a T+1 strategy. It is in the **sandbox** book so no real money is exposed, but the exit path is not clearing the lot, and `futures_follow_entry` has produced no new signal since 2026-07-30 while firing `ok` daily. Worth a look when you have a window — not a pre-open blocker.

### Other observations

- `log/errors.jsonl` is dominated by expired-token noise (122× `Incorrect api_key or access_token` from `broker.zerodha.api.data`) — expected pre-login, clears on re-auth.
- Recurring: `open15: 3 watched symbols have NO NFO option contracts — SCANNER_SYMBOLS is stale: EXIDEIND, NUVAMA, SAMMAANCA`.
- `journal_reflection: nightly run crashed` and several `telegram_bot_service` broadcast failures (`Event loop is closed`) also present.
- open15 (live) last two sessions: Fri 08-14 one real trade `CUMMINSIND` long, **net −₹4,445.82**; Thu 08-13 one real trade `ASHOKLEY` long, **net +₹1,438.16**.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~03:00 IST token expiry). The token is currently expired — historical fetch, quotes and the boot convergence are all failing on it right now.
- Today: **open 09:15 / close 15:30**. sector_follow entry **15:05**, exit **15:10** (post-#512 window, not 15:20).
- futures_follow entry 15:20 / exit 15:25 / watchdog 15:28; scanner_comparison_eod 15:45; postmarket_review 17:15.

(Dheeraj — add EOD notes below in your own time.)
