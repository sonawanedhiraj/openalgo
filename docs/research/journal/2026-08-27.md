# 2026-08-27 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
- **2 commits** on `dev` (clean vs `origin/dev`, 0 unpushed):
  - `3f70434be` — Merge PR #683 from `feat/682-open15-waiting-and-holiday-gate`
  - `0aa9a7a15` — `[#682] feat(open15)`: pre-open "waiting" feed state + holiday gate at the arm
- No `WIP` / `fix(test):` / `refactor:` commits in the window. Nothing to flag.

### Working tree state
- **99 entries** in `git status --porcelain` — 2 modified, ~97 untracked.
- Modified:
  - `.gitignore`
  - `strategies/simplified_engine/LEARNINGS.md` — **~1,427 uncommitted insertions.** Per CLAUDE.md, strategy-learnings updates go **direct to `dev`**. Largest piece of unsaved work in the tree.
- Untracked bulk is research scratch, not source: `backtest/options_open15/*` (~20 scripts + parquet caches), `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/inhouse_scanner/r60/`, `backtest/news_event_study/*`, plus `audit/open15_replay_removal_backup_20260817_165557.json`.
- Also sitting in `db/`: **11 `*.bak.*` snapshots** (9 × `openalgo.db`, 2 × `sandbox.db`, 14 Jul → 18 Aug). The two repair snapshots (`20260817_624repair`, `20260818_194611_626repair`) are worth keeping; the older seven are dead weight.

### Pre-market freshness
⚠️ **Convergence in progress, not yet clear — recheck at ~09:20 IST.**

- Boot convergence check ran **08:44 IST** (`2026-08-27 03:14 UTC`) and reported `overall_ok=0` for **both** scanner arms — `scanner_universe_1m` and `scanner_universe_D`, **216 symbols stale each** (the whole universe).
- **This is the normal boot sequence, not (yet) a failure.** Yesterday shows the identical shape: 09:07 IST → 216 stale, then converged to `[]` by 09:18 IST and stayed `overall_ok=1` all day through the 17:45 IST post-close pass.
- `sector_follow_cap5_vol` last verdict: **ok**, `stale_symbols=[]` (16:30 IST yesterday). No sector_follow check has run yet today.
- **Action:** if `scanner_universe_*` is still `overall_ok=0` after ~09:20 IST, the overnight/boot backfill has genuinely failed and the scanner will be running on stale D-bars.
- *(Note: `historify.duckdb` could not be read directly this run — the duckdb module isn't available in the sandbox and the repo venv is Windows-only. Freshness above is taken from `data_health_check`, which is the strategy's own verdict on the same data.)*

### Scheduler / overrides

**Scheduler — healthy.** Every expected job fired `ok` yesterday, none missed:
- `sector_follow_*`: `daily_reset` 09:00, `preentry_refresh` 15:02, `smoke_check` 15:03, `entry` 15:05, `exit` 15:10, `eod_summary` 15:30 — all ✅
- `futures_follow_*`: `daily_reset` 09:00, `smoke_check` 15:18, `entry` 15:20, `exit` 15:25, `eod_watchdog` 15:28, `eod_summary` 15:30 — all ✅
- `open15_*`: `arm` 09:10, `first_candles` 09:16, `entry_verify`/`exit` 09:30, `exit_retry` 09:32, `summary` 09:35 — all ✅
- `scanner_comparison_eod` 15:45 ✅ · `option_liquidity_eod` 15:45 ✅ · `postmarket_review` 17:15 ✅ · `multi_account_eod_summary` 15:35 ✅ · `multi_account_fill_reconcile` 09:40 ✅

**Overrides — none active.** Three `strategy_runtime_override` rows exist but all expired `2026-08-12 10:00` (the 12 Aug smoke-check failures on `sector_follow_cap5_vol`, `futures_follow_cap50`, `intraday_pullback_top2`). Nothing is holding entries today.

**Modes:** `open15_vol_breakout` = **live** · `sector_follow_cap5_vol`, `futures_follow_cap50`, `simplified_engine` = sandbox.

### 🔴 Standing P0 — futures_follow_cap50 carry (#497 shape, recurring)

`postmarket_review` has fired the **same** contract violation with the **same fingerprint** (`38bd638a20a9`) on **8 consecutive review days** — 13, 14, 17, 18, 19, 20, 25, 26 Aug:

> `futures_follow_cap50 / t1_exit_for_carry` — **P0** — 3 lots open, oldest entry **2026-07-30 (27 days)**, **0 exits today**, 2 entries today.

On a **T+1** strategy. Entries keep placing, exits are not clearing the carried lots. It's `mode=sandbox`, so this is virtual money — but it is the exact failure shape that stranded 7 NIFTY lots at 110% of book in the 27–30 Jul window (#497), and the detector has been shouting about it for two weeks with nothing acted on. `futures_follow_trades` shows several recent BUY rows with `exit_price = NULL`.

Not actioned here (this standup is read-only). Worth an issue + a look at whether `rehydrate` / `run_exit` is resolving the sandbox position book.

### Reminders
- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~06:30–07:30 token flush; auto-login watcher covers it but verify).
- Today: **open 09:15 / close 15:30**; `open15` arm 09:10 → exit 09:30; `sector_follow` entry **15:05** → exit 15:10 (post-CAS timing, #512); `futures_follow` entry 15:20 → exit 15:25.

(Dheeraj — add EOD notes below in your own time.)
