# 2026-09-01 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
- **0 commits** since Fri 2026-08-28. Monday 08-31 produced no commits.
- Last commit on `dev`: `34052675c` (2026-08-28) — merge PR #689 `fix/688-auto-login-notify-importerror`
  ("silently-dropped alerts — three sites imported a `notify` that never existed").
- Nothing flagged `WIP` / `fix(test):` / `refactor:` in the window.

### Working tree state
- **83 porcelain entries** on `dev` — but only **2 tracked modifications**:
  - `M .gitignore`
  - `M strategies/simplified_engine/LEARNINGS.md`
- The other 81 are untracked scratch: `backtest/options_open15/*` (~19 scripts),
  `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/inhouse_scanner/r60/`,
  `backtest/news_event_study/*`, and ~10 `db/openalgo.db.bak.*` snapshots.
- Nothing load-bearing appears uncommitted. The LEARNINGS.md edit is the only one worth a look.

### Pre-market freshness
- ⚠️ `db/historify.duckdb` could **not** be read from the sandbox — `Permission denied`
  (4.3 GB file held read-write by the live app). Freshness assessed from the
  `data_health_check` table + live logs instead.
- Latest recorded verdicts (all `overall_ok=1`, zero stale symbols):
  - `scanner_universe_1m` — 2026-08-31 15:18 IST ✅
  - `scanner_universe_D` — 2026-08-31 15:18 IST ✅
  - `scanner_smoke_check` — 2026-08-31 09:18 IST ✅
- ⚠️ `sector_follow_cap5_vol` data-health last ran **2026-08-27 16:30 IST** — the daily
  `sector_follow_data_health` job has not fired on 08-28 or 08-31 (see scheduler note below).
- ✅ Daily-`D` convergence is running **right now**: 152 symbols already upserted this
  morning for the 2026-08-28 → 2026-09-01 window (JINDALSTEL, JIOFIN, JSWSTEEL, KALYANKJIL …).
  Historical fetches succeeding is itself evidence the primary broker token is alive.

### Scheduler / overrides
- **Today:** app booted **08:50 IST** (comfortably ahead of the 09:15 ops deadline).
  WS proxy up, scanner pre-subscribed **228/228** symbols, regime 10/10.
  No `job_run` rows yet for 2026-09-01 — expected, first fire is ~09:00 IST.
- **Monday 08-31:** 199 job fires, **0 errors, 0 missed**. All key jobs green —
  `sector_follow_{preentry_refresh,smoke_check,entry,exit,eod_summary}`,
  `futures_follow_{smoke_check,entry,exit,eod_watchdog,eod_summary}`,
  `open15_{first_candles,entry_verify,exit,exit_retry,summary}`,
  `scanner_comparison_eod` (15:45 IST).
- ⚠️ **The 09:00 / 09:10 IST jobs did not fire on 08-31.** Earliest recorded fire that day
  was 09:15 IST (`scanner_dry_tripwire`); `open15_arm`, `sector_follow_daily_reset` and
  `futures_follow_daily_reset` last fired **08-28**. Consistent with an app restart between
  09:00 and 09:15 IST. open15 still traded (arm evidently recovered via the boot /
  #677 scheduler-finalize path) — but the arm was not audited.
- ⚠️ **`postmarket_review` has not fired on 08-28 or 08-31** — last run 2026-08-27 17:20 IST.
  Two consecutive trading days with no post-market review or expectation-contract evaluation.
  This is precisely the dead-schedule shape the job was built to catch.
- ⚠️ Fri 08-28 had 3 `missed` jobs: `sector_follow_preentry_refresh`, `sector_follow_smoke_check`,
  `sector_follow_entry` (all coalesced at 15:11 IST) — Friday's sector_follow entry never ran.
- ✅ **No active runtime overrides.** The only 3 rows in `strategy_runtime_override` all
  expired 2026-08-12 10:00.
- **Modes:** `open15_vol_breakout` = **LIVE** (real money); `sector_follow_cap5_vol`,
  `futures_follow_cap50`, `simplified_engine` = sandbox.

### Broker session — needs eyes before 09:10
- `08:50:50` `Primary Zerodha account auto-login succeeded.`
- `08:51:18` ❌ `Primary auto-login failed at web-login step: Kite browser login timed out after 30s.`
- `08:51:29–30` three × `403 Forbidden` on `/portfolio/positions` (`Incorrect api_key or access_token`) —
  these came through `account_open15_service` (child-position reads), before the children logged in.
- `08:51:34 / 08:51:42 / 08:51:48` ✅ all three child accounts (Swapna, Mai, Didi) auto-login succeeded.
- **Net read:** the primary session looks alive (historify pulls at 08:57 require a valid token),
  but the watcher logged both a success and a failure inside 30 s. **Confirm on `/broker` before 09:10.**
- ⚠️ `08:50:24` `sector_follow rehydrate: position book UNREADABLE — 4 journalled position(s)
  NOT rehydrated (KOTAKBANK, M&M, TATASTEEL, VEDL). No square-off will be attempted.`
  This fired during the token gap and should self-heal on `broker_session_refreshed` —
  worth verifying, since an un-rehydrated book means no T+1 exit for those four.

### Yesterday's live result (open15, real money)
| date | fill | n | net P&L |
|---|---|---|---|
| 2026-08-31 | real | 2 | **−₹10,562.57** |
| 2026-08-31 | paper | 1 | +₹20,014.70 |
| 2026-08-28 | real | 3 | +₹11,705.07 |
| 2026-08-28 | paper | 1 | +₹2,372.24 |
| 2026-08-28 | sim | 2 | −₹1,645.47 |
| 2026-08-27 | real | 3 | +₹11,510.69 |

- Monday's real trades: HDFCBANK L −₹6,500 gross, LTF S −₹3,150 gross.
- ⚠️ **KAYNES (08-31) and PREMIERENE (08-28) were both broker-rejected** with
  *"MIS LIMIT orders are blocked … open interest (OI) being less than 500 lots"* — the
  #595 broker-OI watch-list filter is letting these through two days running, and KAYNES
  would have been the day's best trade (+₹20k paper). Worth a look at whether the
  batched-quote OI read is returning 0/unknown and failing open.
- Note: 4 watched symbols had no NFO option contracts and were dropped from Monday's
  universe (#647 working as intended).

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily token expiry; auto-login was flaky at boot today)
- Today: open 09:15 / close 15:30 / open15 arm 09:10 / sector_follow entry 15:05 / exit 15:10

---

(Dheeraj — add EOD notes below in your own time.)
