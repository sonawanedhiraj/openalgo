# 2026-09-14 Trading Journal

## 08:45 IST standup

> ⚠️ **Degraded run — 3rd trading day in a row.** The sandboxed Linux shell failed to
> mount again (`Plan9 share "c" not mounted`, 3 identical failures — a Windows update
> released 2026-09-08 is the stated cause). So **no `git`, no `duckdb`, no `python`, and
> no Telegram send.** Everything below is reconstructed read-only from files:
> `.git/logs/HEAD` + per-worktree reflogs, and `log/openalgo_2026-09-11.log`.
> See "Gaps" at the end.

---

### 🔴 HEADLINE — OpenAlgo has been DOWN since Friday 09:32 IST

`log/openalgo_2026-09-11.log` ends with `Received signal 2, initiating graceful
shutdown` at **09:32:13** on Friday. There is **no log file for 09-12, 09-13 or
09-14** — the app has not been started since.

Three consequences:

1. **It is not running right now (08:45).** The open15 **09:10 arm will not happen**
   unless you boot it. Friday's boot convergence held the lock for **778 s (~13 min)** —
   so a 09:00 boot leaves convergence still running into the arm. **Boot by ~08:50.**
2. **Friday's whole afternoon schedule never fired**: `sector_follow` 15:05 entry /
   15:10 exit, `futures_follow` 15:20/15:25, `scanner_comparison_eod` 15:45,
   `option_liquidity` EOD sweep 15:45, `sector_follow` data-health 16:30,
   `postmarket_review` 17:15. This is now the **third** consecutive occurrence of the
   missed-evening-jobs pattern (09-10, 09-11, 09-12).
3. **Friday's open15 09:35 summary job never ran** (shutdown at 09:32). The #641
   primary fill-reconcile runs at the tail of `flatten` (09:30) so P&L should be
   fill-true, but the day digest / summary event and the bars-backfill backstop are
   missing for 09-11 — the `/logs` page for Friday may render an incomplete digest.

---

### Last trading day (Fri 2026-09-11) — open15 result: **−₹5,978.93**, 2 trades, both stopped

Armed **live**, universe 207, `vol_mult 1.50`, `max_vol_ratio 1.80`, `top_n 3`,
window 09:16 → `no_entry_after 09:24` → exit 09:30, `both` sides, `atm_option`,
3 slots × ₹60,000, available cash ₹2,20,900.40, rolling watch-list on (15 s cadence).

| Symbol | Src | Contract | Entry | Exit | Reason | Net P&L |
| --- | --- | --- | --- | --- | --- | --- |
| ONGC (L) | seed | `ONGC29SEP26242.5CE` 6×2250 | 4.13 @ 09:20:51 | 3.95 @ 09:21:14 | `stop_loss` | **−₹2,963.22** |
| TECHM (L) | rolling | `TECHM29SEP261540CE` 2×600 | 43.00 @ 09:22:31 | 40.90 @ 09:23:39 | `stop_loss` | **−₹3,015.71** |

**Both stops were RIGHT** (`stop_counterfactual`, source=live, held to 09:30):
ONGC saved **+₹134.36**, TECHM saved **+₹1,313.72**. TECHM's MFE never went positive
(−₹2,844) — that trade was wrong from the tick. Feed both into the #704 stop scorecard.

**#721 and #722 were both live on their first day:**

- **#721 volume ceiling fired once** — `HINDALCO` short skipped at `vol_ratio 2.14`
  vs the 1.80 ceiling, journalled `entry_skipped · vol_ratio_cap`, sim-priced at 1 lot,
  no slot consumed. First data point against the pre-registered ~80-fill rule.
- **#722 per-side stops were armed** — `stop_loss_inr_long 3000` /
  `stop_loss_inr_short 2500` (common 2500). Both fills were LONGs and both stopped
  right at the ₹3,000 long threshold, which is the behaviour #722 shipped for.

Also seen at the arm: 4 symbols dropped as `not_in_fno` (**DALBHARAT, EXIDEIND,
NUVAMA, SAMMAANCAP** — `SCANNER_SYMBOLS` is stale, #648/#647 working as designed), and
repeated `oi_below_broker_min` exclusions (INDUSTOWER 287 lots, COCHINSHIP 310,
GODREJPROP 429, LODHA 232) — #595 doing its job.

### Git deltas since Friday's standup — 1 fast-forward, nothing since

| When (IST) | What |
| --- | --- |
| Fri 08:48:44 | `d8f215b` in `wt-722` — merge dev (#721 ceiling) into the per-side-stop branch |
| Fri 08:57:47 | `dev` **`674c6f8` → `4781088`** (`pull: Fast-forward`) — **#722 per-side stops merged** |
| Fri 08:58:20 | OpenAlgo restarted on the new HEAD (so Friday's 09:10 arm ran WITH #722) |

**Nothing after Friday 08:57.** No commits Friday during/after the session, none over
the weekend, no new worktrees past `wt-716` / `wt-721` / `wt-722`.

**Resolves Friday's open item:** #722 is no longer branch-only — it is merged and was
live on Friday.

**Flag for awareness:** no `WIP`, no `fix(test):`, no `refactor:`. But Friday's session
produced **zero commits** — the 09-11 journal itself is still unwritten-to (no EOD
notes) and untracked.

### Working tree state — ✅ verified, unchanged from Friday

Source: `app.py` boot dirty-check at **08:58:20 Fri** (post-pull). 101 entries.

| Status | Path | Note |
| --- | --- | --- |
| `M ` | `.gitignore` | **Staged** in the index — still the one item that looks like an interrupted intent |
| ` M` | `strategies/simplified_engine/LEARNINGS.md` | Unstaged edit |

The other 99 are untracked scratch/output: ~20 `backtest/options_open15/*.py` +
`open15_rolling/`, `open15_missed_days/`, `inhouse_scanner/r60/`, `news_event_study/`;
**13 × `db/*.db.bak.*`** going back to 2026-07-14 (worth pruning); ~30 ×
`docs/research/journal/*.md` (2026-08-21 → today, never committed per the audit-trail
policy); 24 × `log/restart_*.{out,err}`. No lost work.

### Pre-market data freshness — ⚠️ inferred, one real risk

- ❌ **Could not query `db/historify.duckdb` directly** (no shell / no duckdb).
- **Friday's post-close convergence never ran** (app down from 09:32), so Friday's 1m
  bars for the scanner universe and the sector_follow universes were never backfilled
  after the close. Today's **boot convergence should self-heal it** — the incremental
  pipeline fetches from each symbol's last bar forward, and the daily-`D` resettle
  window (last 2 settled trading days) covers Friday. **But it only runs if you boot.**
- Friday's own boot resettle was clean: daily-`D` 2026-09-09..09-11 over 216 symbols,
  provider cache refreshed (212 symbols, 0 errors), prev-close registry 211/216
  (5 skipped, 0 errors).
- The `216/216 still stale` warnings on both the 1m and `D` arms at 08:49/08:50 are
  **expected pre-open** — they chase *today's* bars before the market has opened.
- ⚠️ **Option-liquidity coverage is now 4 days stale** — the 15:45 EOD sweep last ran
  2026-09-03 and has missed 09-10, 09-11, 09-12. The #591 coverage ladder consumed by
  the 09:10 arm is overstated. The 1200 s `option_liquidity` convergence loop may heal
  it after boot; **glance at the coverage card on `/open15_vol_breakout/logs` before
  09:10.**

### Scheduler / overrides

Last verified registration (Friday 08:58 boot — **not re-verified today, nothing is
running**):

| Strategy | Jobs (IST) | Mode |
| --- | --- | --- |
| `open15_vol_breakout` | arm **09:10** / first-candles 09:16 / exit **09:30** / retry 09:32 / summary 09:35 / entry-verify each min | **live** (confirmed by Friday's `armed` event) |
| `sector_follow_cap5_vol` | entry 15:05 / exit 15:10, `strategy_id=1` | sandbox |
| `futures_follow_cap50` | smoke 15:18 / entry 15:20 / exit 15:25 / watchdog 15:28, `entry_mode=legacy` | sandbox |
| `scanner_comparison_eod` · `option_liquidity` EOD | **15:45** mon-fri | — |
| `postmarket_review` | **17:15** | — |

- `futures_follow` Stage-1 LLM veto: enforcement **off**.
- **Overrides: none surfaced.** `strategy_runtime_override` initialised with nothing
  logged as held on either Friday boot. ⚠️ Could not query the table directly.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST.** OpenAlgo is down, so the auto-login watcher
  is not running either — **booting the app is what triggers it** (Friday: primary
  08:38:03, child `acct:3` 08:38:12, fully automatic). Verify on `/broker`.
- Today: open **09:15** / close **15:30**.
- open15 window **09:16–09:24 entries**, exit **09:30** (arm **09:10**) ·
  sector_follow entry **15:05**, exit **15:10** · futures_follow entry **15:20**,
  exit **15:25**.
- ⚠️ The SKILL's stated "sector_follow entry 15:20" is **stale** — it moved to **15:05**
  on 2026-08-03 for the NSE closing-auction change (#512).

### Gaps in this run

1. **Shell mount failure three trading days running** (09-10, 09-11, 09-14). The
   standup cannot run `git`, `duckdb`, or the Telegram send from its own spec. Today
   there was also **no fresh boot log to fall back on** — all system state below is
   Friday's. This needs a fix, not a workaround.
2. **No Telegram sent** — the bot token requires `database.telegram_db.get_bot_config()`
   decryption, which needs Python. This file is the only delivery today.
3. Not verified: `historify.duckdb` freshness, `strategy_runtime_override` rows,
   and anything about the system's state *today*.

### Suggested first 20 minutes

1. **Boot OpenAlgo now** — nothing else on this list matters otherwise, and convergence
   needs ~13 min before the 09:10 arm.
2. Check the option-liquidity coverage card before 09:10 — the sweep last ran 09-03.
3. Decide whether Friday's two long stops at the ₹3,000 threshold argue for revisiting
   `stop_loss_inr_long` — both were right, but both were also the whole day.
4. Look at why the app stops ~09:32 every day; the evening jobs have now missed three
   sessions running, and `postmarket_review` is the layer meant to catch exactly this.
5. Deal with the staged `.gitignore`.

(Dheeraj — add EOD notes below in your own time.)
