# 2026-07-31 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
- 4 commits (accounts + strategies work; no `WIP` / `fix(test):` / `refactor:` flags):
  - `36a74e088` [#497] **fix(mode)**: resolve a strategy position read with its own order mode — the subject line landed as a stray `@ (#499)` merge artifact, but the body is the #497 fix. ⚠️ Meaningful: `futures_follow_cap50` had placed entries every trading day from 2026-07-27 and exited **none** of them — 7 NIFTY lots stranded in `sandbox.db` at ~110% of book vs a 50% cap (last T+1 exit 2026-07-20). This fix makes a strategy read its own book with the same mode that routed the order. Sandbox-only impact, now resolved.
  - `094191f9f` [#496] feat(accounts): capital-per-trade mirror sizing — replaces the ratio model (#498)
  - `3ba88925f` [#492] fix(accounts): TOTP copy button + guard child credentials against autofill clobber (#493)
  - `c6c0827f1` [#494] feat(strategies): long/short P&L breakup for the simplified engine (#495)

### Working tree state
- **61 entries** — 2 tracked-file modifications, 59 untracked.
  - Modified: `.gitignore`, `strategies/simplified_engine/LEARNINGS.md`
  - Untracked: mostly accumulated journal files (`docs/research/journal/*.md` back to 2026-07-08), new `backtest/` subdirs (`inhouse_scanner/`, `options_open15/`, news-event-study scripts), and two `db/*.bak.20260714` backups. Nothing looks accidentally important — but the LEARNINGS.md and .gitignore edits are uncommitted; commit at your discretion.
  - Note: `.git/index.lock` is present and unremovable from this sandbox (mount permissions) — harmless here, but if a local git op complains, remove the stale lock.

### Pre-market freshness
- **Fresh at yesterday's close, all-stale this morning as expected.**
  - `scanner_universe_1m` / `scanner_universe_D`: `overall_ok=1`, 0 stale at **2026-07-30 17:40** (post-close convergence).
  - `sector_follow_cap5_vol`: ok at 2026-07-30 11:00.
  - This morning's **03:07 IST** check reads **ok=0, 216/216 stale** for both scanner universes — this is the normal post-3 AM token-expiry state (no broker session to fetch today's data). It will **self-heal via boot+periodic convergence once you re-login**. Not an overnight failure.

### Scheduler / overrides
- **App is running** — `health.db` written 08:51, `openalgo.db` 08:45 this morning.
- **Zerodha session is DOWN (token expired ~3 AM as expected).** 18 error entries today, all token-expiry symptoms: WS `403 Forbidden`, `Auth/token failure detected — will not retry`, ping/pong timeouts, `multiquotes` failure, and one `/zerodha/callback` 500 (~01:36). → **Re-login required before 09:15.**
- **Jobs:** sector_follow + scanner_comparison_eod register at boot (app is up, no boot errors) — treated as armed.
- **Active overrides: NONE.** Both `strategy_runtime_override` rows are long expired (2026-07-01, 2026-07-21). Nothing holding entries today.
- **Modes:** `sector_follow_cap5_vol = LIVE`, `open15_vol_breakout = LIVE`, `futures_follow_cap50 = sandbox`, `simplified_engine = sandbox`. ⚠️ **Two strategies are LIVE** — sector_follow (15:20 entry) and open15 (09:15 window). Real orders today.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily 3 AM token expiry) — scanner/sector_follow freshness can't converge and the WS feed stays 403'd until the session is live.
- Today: open 09:15 / close 15:30 / sector_follow entry 15:20.

(Dheeraj — add EOD notes below in your own time.)
