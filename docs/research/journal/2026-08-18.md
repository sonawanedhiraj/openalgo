# 2026-08-18 Trading Journal

## 08:45 IST standup

_(Companion to `2026-08-18_morning_status.md` (08:25 IST) — that file has the deep
dive; this is the 30-min-to-open state-of-system.)_

### Yesterday's deltas

**12 commits** since 2026-08-17, all on the open15 / intraday_pullback line:

| Commit | Summary |
| --- | --- |
| `9c32809bd` | `[#624]` fix(intraday_pullback): resume replay must rebuild state, not re-decide the day |
| `78a769e3a` | `[#622]` fix(open15): restore `renderRejected` — the replay removal deleted it |
| `7cc6bf792` | `[#620]` chore(open15): remove the replay feature |
| `ce384b7cd` | `[#617]` fix(open15): replay config fallback no longer KeyErrors on a never-armed day |
| `04a22c32f` | `[#612][#615]` fix(open15): clobbered-log days render their trades; replay rows state their trigger |
| `3346d53d5` | `[#613]` fix(open15): replay POST sends X-CSRFToken — the button never worked |
| `383568602` | `[#608]` fix(open15): a day that SELECTED is not replayable, whatever else its log holds |
| `2cdaff8e2` | `[#606]` fix(open15): replay eligibility rides the days digest, no per-card fetch |
| `b812bfd39` | `[#604]` feat(open15): replay endpoints + the replay button on the logs page |
| `1b0d7ac2b` | `[#602]` fix(open15): `opt_entry_premium_early` belongs to `open15_trades`, not `open15_config` |
| `2279218d8` | `[#600]` feat(open15): replay engine + `fill='replay'` bucket for missed sessions |
| `4de412877` | docs(open15): round 62 — 1m reconstruction of the 2026-08-12/08-17 missed sessions |

**Flag for awareness:** the whole replay feature was **built and then removed inside
one day** (`#600` → `#621`), with two follow-up fixes after the removal (`#622`,
`#624`). 9 of 12 commits are `fix(...)`. Nothing labelled `WIP`. The removal left a
regression that `#622` had to restore — worth a glance that nothing else went with it.

### Working tree state

**153 entries**, but only **2 tracked modifications**:

- `M .gitignore`
- `M strategies/simplified_engine/LEARNINGS.md`

The other **151 are untracked** — backtest scratch (`backtest/open15_missed_days/`,
`backtest/open15_rolling/`, `backtest/options_open15/*.py`,
`backtest/inhouse_scanner/`, `backtest/news_event_study/*.py`), several
`db/openalgo.db.bak.*` snapshots, and
`audit/open15_replay_removal_backup_20260817_165557.json`.

Nothing important looks at risk of being lost, but the modified `LEARNINGS.md` is a
canonically-tracked file — commit it at your discretion.

### Pre-market freshness

⚠️ **Caveat on method:** `db/historify.duckdb` could not be read directly — the Cowork
sandbox has no `duckdb` module and no PyPI network to install one, and the file is
open read-write by the running app (4.0 MB `.wal` present). Freshness below is read
from `data_health_check` in `openalgo.db`, which is the app's own derived verdict on
the same DuckDB.

| Check | Last run | Verdict |
| --- | --- | --- |
| `scanner_universe_1m` | 2026-08-18 08:49 IST | `ok=0`, **216/216 stale** |
| `scanner_universe_D` | 2026-08-18 08:49 IST | `ok=0`, **216/216 stale** |
| `scanner_universe_1m` / `_D` | 2026-08-17 20:35 IST | `ok=1`, 0 stale |
| `sector_follow_cap5_vol` | 2026-08-17 16:30 IST | `ok=1`, 0 stale |
| `scanner_dry` | 2026-08-17 15:25 IST | `ok=1` |

**Read:** the 08:49 all-216-stale result is almost certainly a **pre-open artifact**,
not an overnight failure — the staleness predicate measures symbols against *today's*
expected close, and at 08:49 no symbol can have 2026-08-18 data. The same universe was
fully fresh at 20:35 IST last night and nothing runs overnight to undo that.
sector_follow's own universe (the one that gates entries) reported clean at its 16:30
job.

**Not proven, though.** If you want certainty before 15:05, `GET
/sector_follow_cap5_vol/api/data_health` gives the live per-symbol answer, and the
15:03 smoke check will hard-gate entries if the aggregator coverage is genuinely bad.

### Scheduler / overrides

**Overrides: none active.** The three most recent `strategy_runtime_override` rows are
all smoke-check auto-pauses from **2026-08-12** that expired the same morning
(`futures_follow_cap50`, `sector_follow_cap5_vol`, `intraday_pullback_top2`). Nothing
is holding entries today.

**Modes:** `open15_vol_breakout` = **live** (real money); `simplified_engine`,
`sector_follow_cap5_vol`, `futures_follow_cap50` = sandbox.

**Jobs — 08-17 fired clean, 0 errors across the last 7 days.** Both jobs the standup
asks about are armed and were healthy yesterday:

- `sector_follow_*` — full set fired `ok`: `preentry_refresh` 15:02, `smoke_check`
  15:03, `entry` 15:05, `exit` 15:10, `eod_summary` 15:30, `data_health` 16:30,
  `index_backfill` 16:05.
- `scanner_comparison_eod` — fired `ok` at 15:45 IST.

**Two things to know before open:**

1. 🔴 **`open15_arm` did NOT fire on 2026-08-17.** `open15_exit`, `open15_exit_retry`
   and `open15_summary` all ran, but the 09:10 arm did not — its last successful fire
   was **2026-08-14**. This matches the round-62 doc on the missed 08-12/08-17
   sessions. open15 is the **only strategy routing real money**, so a third missed
   session costs the live cohort another day of data. Per CLAUDE.md the arm needs
   OpenAlgo up before 09:15; the app is running now (health.db and the DuckDB WAL both
   written at 08:49), so the arm should fire — **worth eyeballing `/open15_vol_breakout/logs`
   just after 09:10 to confirm it actually armed.**

2. 🔴 **`futures_follow_cap50` P0 carry, day 4.** Yesterday's `postmarket_review`
   fired `t1_exit_for_carry` again (same fingerprint `38bd638a20a9` as 08-14): **1
   NIFTY lot open since 2026-07-30 — 18 days on a T+1 strategy, 0 exits.** Sandbox
   book, so virtual money, but it is the #497 shape and NRML has no MIS square-off
   underneath it. Covered in more detail in this morning's 08:25 status file.

No `job_run` rows for 2026-08-18 yet — expected, the 09:00 IST daily resets are the
first of the day.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** — daily ~3 AM IST token expiry. Auth rows
  exist for `dheeraj.sonawane`, `admin` and `acct:1` (child), but a row is not proof
  of a valid token; the child account `acct:1` needs its own Kite login on `/accounts`
  if you want mirroring today.
- **Today:** open 09:15 / close 15:30.
- **Entry times (the skill's "sector_follow entry 15:20" is stale — #512 moved it):**
  - `open15_vol_breakout` — arm **09:10**, entry window to 09:29, exit **09:30**
  - `sector_follow_cap5_vol` — refresh 15:02 / smoke 15:03 / entry **15:05** / exit
    **15:10** (moved inside continuous trading for the NSE closing-auction change)
  - `futures_follow_cap50` — smoke 15:18 / entry **15:20** / T+1 exit 15:25 / watchdog
    15:28

---

_Generated read-only by the 08:45 IST scheduled standup. No commits, no git ops, no DB
writes._

(Dheeraj — add EOD notes below in your own time.)
