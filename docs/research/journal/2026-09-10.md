# 2026-09-10 Trading Journal

## 08:45 IST standup

> ⚠️ **Ran late — actual execution 09:39 IST**, not 08:45. Market was already open
> and open15's entry/exit window (09:16–09:30) had *already completed*, so this is a
> post-window report rather than a pre-market brief. Also: the sandboxed Linux shell
> failed to mount this run (`Plan9 share "c" not mounted`), so **no `git` and no
> `duckdb` were available**. Repo history was read from `.git/logs/HEAD`; live system
> state was read read-only over the localhost OpenAlgo API. Working-tree status and a
> direct historify freshness query could not be run — see gaps below.

### Yesterday's deltas (2026-09-09) — 5 commits

| Time (IST) | Commit | Summary |
| --- | --- | --- |
| 09:33 | `a05505f` | `feat(open15)`: continue the intra-hold chart after a profit-trail exit (#713) |
| 09:35 | `51568b1` | `fix(open15)`: widen the intra-hold chart's label column so held/saved labels aren't clipped (#713) |
| 10:26 | `2300a7b` | `[#715] fix(open15)`: an OI-floor broker rejection is never a PAPER fill |
| 15:46 | `58b5caa` | `[#719] fix(auth)`: session expiry preserves a fresh auto-login token; never mint before Kite's 07:30 flush |
| 15:53 | `afc1685` | `[#719] test`: align open15 card + auth-probe tests with the #719 semantics |

Three fast-forward merges into `dev` followed (`9ba5df3` → `6e4d641` → `fcd42ef`),
so **#713, #715 and #719 are all merged**. Current `HEAD` on `dev` = `fcd42ef`.

**Flag for awareness:** nothing marked `WIP`. One `test:` commit (`afc1685`) is a
test-alignment follow-up to a same-day behaviour change (#719) — normal, but it means
yesterday's auth semantics change is only as safe as those realigned tests.

### Working tree state

- ❓ **Not verified this run** — `git status` was unavailable (shell mount failure).
- Known: this journal file is newly created and is an **uncommitted working-tree
  addition**. Per the audit-trail policy the scheduled task does not `git add`/`commit`.

### Today's open15 result (window already closed by the time this ran)

Live mode (`open15_vol_breakout` = **live**, analyze_mode off). Armed 09:10, 15 selected,
9 added by the rolling watch list, 5 triggered, **3 real fills — all three stopped out**.

| Symbol | Side | Contract | Exit | Reason | Gross | Charges | **Net** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| VBL | L | `VBL29SEP26410CE` | 09:20:02 | stop_loss | −114.75 | 500.66 | **−615.41** |
| CANBK | L | `CANBK29SEP26127.5CE` | 09:23:06 | stop_loss | −3,240.00 | 539.60 | **−3,779.60** |
| MCX | S | `MCX29SEP263300PE` | 09:24:27 | stop_loss | −2,700.00 | 429.12 | **−3,129.12** |
| | | | | | | **Real net** | **−₹7,524.13** |

Sim (not traded — `max_trades_cap`): FORCEMOT +₹640.03, GVT&D −₹412.58 → the cap was
mildly *positive* to skip on net, so slot capital was not obviously the binding
constraint today.

Notes worth a second look when you have time:
- **All three stopped inside 9 minutes of the window opening** (09:20–09:24). The risk
  monitor's `risk_path` shows the book underwater from the first poll at 09:21:59.
- **Charges were 24% of the gross loss** (₹1,469 on three round trips) — on a
  small-gross day like VBL (−₹115 gross → −₹615 net) the modelled charges dominate.
- `fill_reconcile` clean: 3/3 reconciled, 0 pending, 0 paper, 0 unfillable.
- 72 `universe_excluded` events, all `oi_below_broker_min` (stage 3) — the #595 filter
  is doing heavy lifting on the rolling additions.

### Pre-market freshness

- ❓ **Not directly verified** — `db/historify.duckdb` could not be queried (no shell,
  no `duckdb` module, `/sector_follow_cap5_vol/api/data_health` requires an API key
  which this read-only session doesn't hold).
- Indirect signal: **`sector_follow_data_health` last fired 2026-09-03 16:30 IST** —
  it has not run on 09-04, 09-07, 09-08 or 09-09. `postmarket_review` shows the same
  pattern (last fire 2026-09-03 17:15 IST). Both are *evening* jobs, so the most likely
  explanation is simply that **OpenAlgo isn't running after market close on those days**,
  not that the jobs are broken. Worth confirming — it also means the 15:30–17:00 backfill
  convergence window has been missed for ~5 trading days, and staleness would go
  unalerted.

### Scheduler / overrides

- **50 catalogued jobs: 40 registered, 7 not_registered, 3 unregistered.** No stale or
  dead threads (67 threads, all healthy).
- `sector_follow_*` fully armed for today: pre-entry refresh **15:02**, smoke **15:03**,
  entry **15:05**, exit **15:10**, EOD summary **15:30**, data health **16:30**.
- `scanner_comparison_eod` armed for **15:45**; `postmarket_review` for **17:15**.
- `futures_follow_cap50` armed: smoke 15:18 / entry 15:20 / exit 15:25 / watchdog 15:28.
- **Modes:** `analyze_mode` OFF. `open15_vol_breakout` = **live**;
  `futures_follow_cap50`, `sector_follow_cap5_vol`, `simplified_engine` = sandbox.
- **No active pause / kill-switch override** surfaced by `/mode/status`
  (`intent: run`, `source: strategy_mode`).

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** (daily token flush; note #719 shipped
  yesterday — a token minted after 07:30 IST is now preserved through cookie expiry).
- Today: open **09:15** / close **15:30** / open15 window **09:16–09:30** (done) /
  sector_follow entry **15:05**, exit **15:10** / futures_follow entry **15:20**.

### Gaps in this run (fix before tomorrow)

1. The scheduled task fired ~54 min late and after open15's whole window — the brief
   lost its pre-market purpose. Check the Cowork task's cron/trigger.
2. Sandboxed shell failed to mount, so `git status` and the duckdb freshness query were
   both skipped. If this recurs, the standup can't do items 2 and 3 of its own spec.

(Dheeraj — add EOD notes below in your own time.)
