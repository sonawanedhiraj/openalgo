# 2026-07-15 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
5 commits on `dev` (all 2026-07-14):
- `252e23b9d` [#394] feat(intraday_pullback): combined long+short sandbox strategy (#408) — new strategy landed.
- `ac3bc0d51` [#406] feat(futures_follow): OPTION_C same-minute@15:25 entry (flag-gated) (#407) — new entry-timing mode, default still `legacy`.
- `7c9894c26` docs(param): bump VETO_CLAUDE_TIMEOUT_SECONDS 25→60 (LLM veto timeouts).
- `259a010d8` [#403] **fix(futures_follow)**: self-heal T+1 exit against rehydration boot-race (#404).
- `ea9f2f20e` [#401] **fix(futures_follow)**: rehydration stamps entry_date as previous trading day (#402).

⚠️ Two consecutive `fix(futures_follow)` commits around T+1 exit / rehydration boot-race — flag for awareness. futures_follow is the active sandbox sleeve; watch today's 15:25 exit / 15:20 entry behaves as expected. No `WIP`, no `fix(test):`, no `refactor:`.

### Working tree state
- Tracked `git diff` reports ~1518 files, but this is **CRLF→LF line-ending noise**, not real edits (pre-existing environmental state — every diff line is a "CRLF will be replaced by LF" warning). Not a meaningful signal.
- **34 untracked files** (real signal): journal docs (`2026-07-08/10/13/14.md`, several `*_morning_status.md`, `2026-07-15_morning_status.md`), research notes (`monthly_options_buy`, `screener r53`), a few `backtest/news_event_study/*.py`, and **two DB backups** — `db/openalgo.db.bak.20260714_175722`, `db/sandbox.db.bak.20260714_175722` (taken yesterday ~17:57). Dheeraj to commit/prune at his discretion.

### ⚠️ Integrity anomalies (verify locally — NOT auto-fixed, read-only run)
- **`.git/HEAD` is null-byte padded** → git can't resolve HEAD ("branch appears broken"). The `dev` ref itself is intact (`252e23b9d…`); this standup read commits via the ref directly. Likely a file-sync artifact. Fix locally if real: `git symbolic-ref HEAD refs/heads/dev`.
- **`db/openalgo.db` reads as "malformed"** through the mount, even on a `cp` snapshot. Most likely the mount is not presenting a consistent snapshot of a live-written 70 MB SQLite DB (torn read), **not necessarily real corruption** — errors.jsonl has fresh 08:12 IST entries, so the app is running normally. Verify locally: `sqlite3 db/openalgo.db "PRAGMA integrity_check;"`.

### Pre-market freshness
- **Could not verify from the sandbox this run.** `historify.duckdb` isn't readable (no `duckdb` module; `uv`/`pip` are offline in this environment) and `openalgo.db` reads malformed through the mount (see above). Recommend checking `GET /sector_follow_cap5_vol/api/data_health` in the app after Zerodha re-login.
- errors.jsonl shows 71 entries today, all pre-market "Could not find instrument token for NSE:BEL / NSE:BHARTIARTL" history-fetch failures at ~08:12 IST — **normal pre-open master-contract-still-loading / pre-re-login noise**, not an incident. Should clear once the daily master contract loads and Zerodha session is re-established.

### Scheduler / overrides
- **Could not query `strategy_runtime_override` from the sandbox** (openalgo.db unreadable via mount). No active pause/kill-switch could be confirmed or ruled out — check `/api/*/status` after login.
- sector_follow + scanner_comparison_eod jobs are wired to register at boot per CLAUDE.md; runtime arming not verifiable from here.

### Telegram
- **Standup was NOT delivered to Telegram this run** — the bot config (chat IDs + token) lives in the encrypted `openalgo.db`, which is unreadable through the mount, and the offline sandbox can't run the decrypt path. This journal file is the durable record; Telegram delivery should resume once the DB reads cleanly.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily 3 AM token expiry). Pre-market token-not-found errors above suggest the session isn't re-established yet.
- Today: open 09:15 / close 15:30 / sector_follow entry 15:20 (futures_follow entry 15:20, T+1 exit 15:25).

(Dheeraj — add EOD notes below in your own time.)
