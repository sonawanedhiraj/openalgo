# 2026-09-11 Trading Journal

## 08:45 IST standup

> ⚠️ **Degraded run — second day in a row.** The sandboxed Linux shell failed to mount
> again (`Plan9 share "c" not mounted`, 3 identical failures), so **no `git`, no
> `duckdb`, no `python`, and no Telegram send**. Everything below was reconstructed
> read-only from files: `.git/logs/HEAD` + per-worktree reflogs for history, and
> `log/openalgo_2026-09-11.log` for live system state. This turned out to cover more
> than yesterday's run managed — the boot dirty-check logs `git status --porcelain`
> verbatim, so working-tree state IS verified today. See "Gaps" at the end.

### Yesterday's deltas (2026-09-10) — 2 commits

Both authored in isolated worktrees (per the parallel-task rule), both `open15` risk-control knobs —
a direct response to the −₹7,524 three-stop day on 09-10.

| Time (IST) | Commit | Worktree | Summary |
| --- | --- | --- | --- |
| 14:27 | `eb5ef61` | `wt-721` | `[#721] feat(open15)`: volume-ratio **ceiling** at the trigger (`max_vol_ratio`), sim-priced skips |
| 14:28 | `a0319e8` | `wt-722` | `[#722] feat(open15)`: **per-side stop loss** (`stop_loss_inr_long` / `stop_loss_inr_short`) |

**Merge state — this is the thing to know this morning:**

- A `pull: Fast-forward` ran at **08:36 IST today**, moving `dev` `fcd42ef` → **`674c6f8`**.
- **#721 IS merged** — `CLAUDE.md:1786` documents `max_vol_ratio` at HEAD, and the boot
  log shows the migration firing: `open15_config: added column max_vol_ratio` (08:37:26).
- **#722 is NOT merged** — `stop_loss_inr_long` appears nowhere in the working tree.
  It exists only on its branch in `wt-722`. If you expected per-side stops to be
  live for today's 09:10 arm, **they are not**.

**Flag for awareness:** nothing marked `WIP`, no `fix(test):`, no `refactor:`. But #721
shipped a live-behaviour change to a **real-money** strategy (open15 is live) with a
pre-registered decision rule that needs ~80 real fills — worth confirming the ceiling
value it armed with today.

### Working tree state — ✅ verified (101 entries)

Source: `app.py` boot dirty-check at 08:37:26, i.e. *after* this morning's pull.

**Only 2 tracked files modified:**

| Status | Path | Note |
| --- | --- | --- |
| `M ` | `.gitignore` | **Staged** (index), not just working-tree — someone `git add`ed this and stopped |
| ` M` | `strategies/simplified_engine/LEARNINGS.md` | Unstaged edit |

The other **99 are untracked (`??`)** and are all scratch/output, not lost work:

- ~20 × `backtest/options_open15/*.py` + `backtest/open15_rolling/`, `open15_missed_days/`,
  `inhouse_scanner/r60/` — research scripts never added
- **13 × `db/*.db.bak.*`** — repair backups going back to 2026-07-14. These are DB
  snapshots sitting in the repo; worth pruning the old ones.
- 29 × `docs/research/journal/*.md` (2026-08-21 → 09-10) — a month of standup journals
  never committed, per the audit-trail policy that this task must not commit them
- 24 × `log/restart_*.{out,err}` — restart captures from issues #444…#638
- `.claude/launch.json`, `url_1345069591.txt`, `db/.fuse_hidden0000003400000001`

**The staged `.gitignore` is the only item that looks like an interrupted intent.**

### Pre-market system state — ✅ healthy (better than the last few days)

OpenAlgo booted **08:37:25 today**. Full chain came up clean:

- 🔑 **Zerodha auto-login already succeeded — no manual re-login needed.**
  Primary `dheeraj.sonawane` at **08:38:03**, child `acct:3` (Didi-zerodha) at **08:38:12**.
  Master contract re-downloaded (last was 09-10).
- **#719 confirmed working in the wild** (08:40:16): *"Session cookie expired … stored
  broker token is fresh (written after today's expiry boundary) — preserved."* This is
  exactly the bug shipped 09-09; the second Kite login/day is gone.
- **Feed is live: 228/228 symbols subscribed.** A reconnect at 08:40:22 re-subscribed
  **228/228** — the #673 pooled-adapter restore path working correctly.
- `tick_liveness` watchdog started (threshold 10m, autoheal on). Resource snapshot:
  1425 handles / 27 TCP / 82 threads — normal.

**The 403 Handshake ERRORs at 08:37:33–08:37:49 are benign** — the adapter tried the
stale overnight token before auto-login completed at 08:38:03, then reconnected. Not an
incident; ignore them if they show up in `errors.jsonl`.

### Pre-market data freshness — ⚠️ one real gap

- Daily-`D` **resettle ran and succeeded**: 2026-09-09..09-11 over 216 symbols, provider
  daily cache refreshed (212 symbols, 0 errors), prev-close registry updated **211/216**
  (5 skipped, 0 errors). So the `yest_d` reference feeding the scanner is settled and current.
- `scanner universe 1m catch-up: 216/216 (100%) still stale` at 08:49 — **this is
  expected pre-open**, not a failure: it is chasing *today's* 1m bars and the market
  hasn't opened. No action.
- ❌ **Could not directly query `db/historify.duckdb`** (no shell / no duckdb). Freshness
  above is inferred from the backfill job's own verified log lines.

**⚠️ Two evening jobs missed yesterday — confirmed, not inferred.** The boot log states:

- `open15 option-liquidity sweep (15:45 IST)` — *missed by 16:52:28*
- `Sector Follow CAP5_VOL data freshness check (16:30 IST)` — *missed by 16:07:28*

Both point to **OpenAlgo not running after market close on 09-10**, which matches
yesterday's note that these jobs last fired 2026-09-03. Consequence for *today*: the
option-liquidity coverage ladder that the **09:10 arm** consumes is stale, so the #591
coverage read may be overstated. Mitigant: the `option_liquidity convergence loop`
(every 1200s) started at 08:37:28 and may heal it before 09:10 — worth a glance at the
coverage card on `/open15_vol_breakout/logs` before the window opens.

### Scheduler / overrides

All jobs registered at boot for today:

| Strategy | Jobs (IST) | Mode |
| --- | --- | --- |
| `open15_vol_breakout` | arm **09:10** / first-candles 09:16 / exit **09:30** / retry 09:32 / summary 09:35 / entry-verify every 1 min | live (yesterday's mode — ⚠️ not re-verified this run) |
| `sector_follow_cap5_vol` | registered, `strategy_id=1` | **sandbox** |
| `futures_follow_cap50` | smoke 15:18 / entry 15:20 / exit 15:25 / watchdog 15:28, `entry_mode=legacy` | **sandbox** |
| `scanner_comparison_eod` | **15:45** mon-fri | — |
| `option_liquidity` EOD | **15:45** mon-fri | — |
| `postmarket_review` | **17:15** | — |
| `scanner_preentry_refresh` | 09:16, enabled | — |

- `futures_follow` Stage-1 LLM veto: enforcement **off**.
- `sector_follow` rehydrate: 4 journal rows (M&M, TATASTEEL, KOTAKBANK, VEDL) flat in the
  sandbox book → **0 open positions**. Clean.
- **Overrides: no active pause / kill-switch surfaced.** `strategy_runtime_override` table
  initialised with nothing logged as held. ⚠️ Could not query the table directly (no
  sqlite client) — absence of a hold in the log is strong but not conclusive.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** — *already done automatically at 08:38 today*
  (primary + child). No manual action expected; verify on `/broker` if in doubt.
- Today: open **09:15** / close **15:30**.
- open15 window **09:16–09:30** (arm 09:10) · sector_follow entry **15:05**, exit **15:10**
  · futures_follow entry **15:20**, exit **15:25**.
- ⚠️ Note the SKILL's stated "sector_follow entry 15:20" is **stale** — it moved to
  **15:05** on 2026-08-03 for the NSE closing-auction change (#512).

### Gaps in this run

1. **Shell mount failure two days running** (09-10 and 09-11). The standup can no longer
   run `git`, `duckdb` or the Telegram send from its own spec. **This needs a fix, not a
   workaround** — today's coverage was luck (the boot dirty-check happened to log
   `git status`), and on a day OpenAlgo hasn't restarted there'd be nothing to read.
2. **No Telegram sent** — the bot token needs `database.telegram_db.get_bot_config()`
   decryption, which needs Python. This journal file is the only delivery today.
3. Not verified: `historify.duckdb` freshness directly, `strategy_runtime_override` rows,
   and open15's current live/sandbox mode.

### Suggested first 20 minutes

1. Check the option-liquidity coverage card before **09:10** — yesterday's 15:45 sweep never ran.
2. Confirm what `max_vol_ratio` (#721) armed with today; it is live on real money as of this morning's merge.
3. Decide whether #722 (per-side stops) should be merged before the window — it is currently branch-only.
4. Deal with the staged `.gitignore`.

(Dheeraj — add EOD notes below in your own time.)
