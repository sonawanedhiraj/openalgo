# 2026-07-21 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
3 commits on `dev` (all merged PRs, open15 + dashboard work):

- `5ee162d4b` — `[#430] fix(open15): Sandbox badge + UI-reachable decision log/settings + mode-toggle wiring (#431)`
- `d5af68349` — `[#428] fix(open15): module-level scheduler callables — unpicklable jobs killed first session (#429)`
- `7bafdad7b` — `[#425] fix(strategies-dashboard): guard null parity_target in strategy detail`

**Flags for awareness:** all three are `fix:` — none are `WIP`. `#428` is the one worth
watching today: the open15 scheduler jobs were unpicklable and died on the first session,
so **today (2026-07-21) is the first clean trading day** on which the open15 arm 09:10 /
exit 09:30 / retry 09:32 / summary 09:35 jobs should actually fire. Per the SPEC, OpenAlgo
must be **booted before 09:15 IST** or open15 skips the day.

### Working tree state
- Branch: `dev`.
- `git status` over the mounted filesystem timed out repeatedly (4.3 GB `historify.duckdb`
  in-tree makes a full scan slow), so this is a best-effort read rather than a live count.
- Last successful scan showed **~30 entries, all but one untracked**:
  - `M strategies/simplified_engine/LEARNINGS.md` — the only tracked modification.
  - Untracked: accumulated `docs/research/journal/*.md` standup files (07-08 → 07-20),
    `backtest/inhouse_scanner/`, `backtest/options_open15/`,
    `docs/research/strategy/monthly_options_buy/`, rotated `log/openalgo_*.log.*` files,
    and two DB backups (`db/openalgo.db.bak.20260714_175722`,
    `db/sandbox.db.bak.20260714_175722`).
- **Nothing important looks at risk of being lost.** The journal backlog is worth a
  housekeeping commit at Dheeraj's discretion; the two ~July-14 `.bak` DB files are
  candidates for deletion.

### Pre-market freshness

| Universe | Last check | Verdict |
| --- | --- | --- |
| `sector_follow_cap5_vol` (8 indices + 30 stocks) | 2026-07-20 11:00 | ✅ OK — 0 stale, `last_date=2026-07-20`, `staleness_days=0` |
| `scanner_universe_1m` | 2026-07-20 16:30 | ✅ OK — 0 stale (post-close convergence clean) |
| `scanner_universe_D` | 2026-07-20 16:30 | ✅ OK — 0 stale |
| `scanner_universe_1m` / `_D` | **2026-07-21 00:38** | ⚠️ **`overall_ok=0`, 216/216 symbols stale** |

**Read on the 00:38 anomaly — probably benign, but verify:** the post-close 16:30 run on
2026-07-20 was completely clean, so the data *through yesterday's close is good*. A
convergence check then ran at 00:38 today and failed to refresh anything, flagging the
whole universe. Most likely cause is that no broker session was live at 00:38 (the Zerodha
token from Monday had not yet been rotated and there is nothing new to fetch at midnight
anyway) — a failed fetch marks every symbol stale. It is **not** evidence of an overnight
data loss.

**However**, this is exactly the shape of the 2026-07-08 straggler incident, so the
09:16 pre-entry refresh and the **09:18 smoke check** are the things to watch. If the
smoke check FAILS this morning it will arm a per-symbol (or total, if >50% stale) scanner
post-hold and no `scan_results` will be written. The mid-session straggler tick
(`SCANNER_STRAGGLER_RECHECK_MIN`, 15 min, 09:20–15:30) should heal it intraday.
`db/historify.duckdb` mtime is 2026-07-20 14:31 — a direct DuckDB read was skipped to
avoid contending with the running app for the file lock.

### Scheduler / overrides

**Overrides — clear.** The only row in `strategy_runtime_override` is a
`sector_follow_cap5_vol` pause from 2026-07-01 (`smoke_check_failed: index coverage 8/10`)
which **expired 2026-07-01 10:00**. No active pause, no kill-switch. Entries are free to
fire today.

**Modes (persistent `strategy_mode`):**

| Strategy | Mode |
| --- | --- |
| `sector_follow_cap5_vol` | 🔴 **live** |
| `futures_follow_cap50` | sandbox |
| `simplified_engine` | sandbox |

**Scheduler evidence — jobs fired on schedule yesterday**, which is the best available
proof they are armed (APScheduler jobs are in-process and not queryable from outside):
- `futures_follow_cap50` wrote a 15:20 eval snapshot for 2026-07-20 (`n_signals=1`,
  `cap_skipped=1`, `vetoed=0`, `intraday_source_counts: quotes=30`) → the entry job ran
  and the batched quote path was healthy (30/30 on `quotes`, 0 fallbacks).
- `sector_follow_cap5_vol` placed a live entry at 15:20 on 2026-07-20.
- The 16:30 data-health job wrote both scanner rows.
- `db/openalgo.db` mtime is 08:55 today → the app process is up and writing.

### ⚠️ Open position carrying into today
- **`sector_follow_cap5_vol` — LIVE — BUY SBIN 47 @ ₹1060.4, entered 2026-07-20.**
  This is a **real-money CNC T+1 hold**; its exit is today's **15:25 IST** MARKET sell.
- `futures_follow_cap50` placed **no** entry on 2026-07-20 (the single signal was
  `cap_skipped`), so there is **no NIFTY futures position** carrying overnight. The last
  futures round-trip (2 lots `NIFTY28JUL26FUT`, entry ₹24,304.0 → exit ₹24,268.3) is closed.

### Reminders
- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~3 AM token expiry). Nothing —
  backfill, scanner, sector_follow entry, or the live SBIN exit — works without it.
  This matters more than usual today: the 00:38 stale flag will only clear once a
  broker session exists.
- 🖥️ **Boot OpenAlgo before 09:15** if it is not already up — open15 needs it, and #428
  means today is the first day its jobs can actually run.
- Today: open **09:15** / close **15:30** / sector_follow entry **15:20** / sector_follow
  T+1 exit **15:25** (SBIN) / futures watchdog **15:28** / EOD summary **15:30**.

---

*(Dheeraj — add EOD notes below in your own time.)*
