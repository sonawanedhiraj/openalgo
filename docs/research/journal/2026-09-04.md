# 2026-09-04 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
- **2 commits** since 2026-09-03:
  - `409c092a2` — Merge pull request #697 from `feat/696-open15-risk-controls`
  - `deafef8fb` — `feat(open15)`: day profit lock + trail, per-trade stop loss, background risk monitor (#696)
- No `WIP`, `fix(test):` or `refactor:` commits — nothing flagged for awareness.
- ⚠️ Note: #696 ships **day profit lock / trail / per-trade stop loss** on `open15_vol_breakout`, which is the one strategy running **LIVE**. Both rules default OFF (`OPEN15_PROFIT_LOCK` / `OPEN15_STOP_LOSS`); if you want them active today they must be enabled from `/open15_vol_breakout/logs` **before the 09:10 arm** — thresholds are arm-frozen.

### Working tree state
- **88 entries** in `git status --porcelain`.
- Tracked modifications (2): `.gitignore`, `strategies/simplified_engine/LEARNINGS.md`.
- Remaining 86 are **untracked** research/backtest scratch — `backtest/options_open15/*` (~15 scripts), `backtest/inhouse_scanner/r60/`, `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/news_event_study/*`, `audit/open15_replay_removal_backup_20260817_165557.json`, `.claude/launch.json`.
- App logged the dirty tree at boot (08:50:28) as expected.

### Pre-market freshness
- **Yesterday's post-close state was clean.** `sector_follow_cap5_vol` data-health at 16:30 IST 09-03: `overall_ok=1`, every index + stock at `staleness_days=0`, `last_date=2026-09-03`. Scanner `1m` and `D` both `overall_ok=1`, 216/216 fresh.
- ⚠️ **This morning's boot convergence ran BEFORE the Zerodha login and could not fetch.** Sequence:
  - 08:50:24 — OpenAlgo boot
  - 08:51:07–08:51:40 — sector_follow boot convergence: index **8/8 still stale**, stock **30/30 still stale**, `errors=2`
  - 08:52:04 — `scanner_universe_D` health row: **216/216 still stale**, `errors=["no api key available"]`
  - 08:53:22 — **Zerodha session came up**; broker adapter reconnected, **228/228 symbols re-subscribed**, master contract loaded (107,862 symbols)
- Read as a **pre-login timing artifact**, not an overnight failure — the convergence fired ~2 min ahead of the session and had no API key to fetch with. Yesterday's clean 16:30 rows are the last verified truth.
- **What retries, and when:** the scanner straggler tick (09:20–15:30 IST, every 15 min) will re-run the scanner `1m`/`D` convergence once the session is live. The **sector_follow** convergence periodic loop is windowed **15:30–17:00 only** — it will *not* self-retry before the afternoon entry. The **15:18 smoke check** is the backstop and will write a same-day `pause` override if aggregator coverage is genuinely short. Historically (2026-08-12) this exact shape produced a smoke-check pause on all three strategies.
- 🔎 **Watch item:** confirm at ~09:30 that the straggler tick cleared the scanner staleness (new `data_health_check` rows with `overall_ok=1`).
- Note: `db/historify.duckdb` was not directly readable from the standup sandbox (file locked by the running app — expected). Freshness above is sourced from `data_health_check` rows in `openalgo.db`.

### Scheduler / overrides
- **All jobs registered at boot (08:50):**
  - `sector_follow` jobs — mode `sandbox`, strategy_id 1
  - `futures_follow` jobs — mode `sandbox`, entry_mode `legacy`, smoke_check ON
  - `intraday_pullback` jobs — mode `sandbox`
  - `open15` — 6 jobs (arm 09:10 / first-candles 09:16 / exit 09:30 / retry 09:32 / summary 09:35 / entry-verify every 1 min)
  - `scanner_comparison_eod` — 15:45 IST mon-fri ✅
  - `postmarket_review` — 17:15 IST ✅
  - `scanner_history_refresh` — 16:00 IST
- **Active overrides: NONE.** `strategy_runtime_override` holds 3 rows, all from 2026-08-12 and all expired (`expires_at=2026-08-12 10:00`). Nothing is paused.
- **Modes (`strategy_mode`):**
  - `open15_vol_breakout` → **LIVE** ⚠️ (real money, since 2026-07-24)
  - `sector_follow_cap5_vol` → sandbox
  - `futures_follow_cap50` → sandbox
  - `simplified_engine` → sandbox
- `errors.jsonl`: **0 entries today** (file truncated to last 1000 at boot, as designed).

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST — **already done at 08:53 IST today**, session confirmed live (228/228 symbols subscribed).
- Today: **open 09:15 / close 15:30**.
- Strategy timings: `open15` arm **09:10**, entry window to 09:29, exit **09:30**. `sector_follow_cap5_vol` entry **15:05** / exit **15:10** (moved from 15:20 by issue #512 — NSE closing auction). `futures_follow_cap50` entry **15:20** / T+1 exit **15:25** / watchdog 15:28.

_(Dheeraj — add EOD notes below in your own time.)_
