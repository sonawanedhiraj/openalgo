# 2026-09-17 Trading Journal

## 08:45 IST standup

> ⚠️ **Timing caveat — this run fired LATE.** Task clock read **2026-09-17
> ~22:54 IST** when it executed (host file mtimes corroborate: `logs.db`
> 19:30, `historify.duckdb` 13:21). So this is a **post-close** report for a
> day that has already traded, not a pre-market brief. Everything under
> "Pre-market freshness" below is therefore an *end-of-day* freshness read.
>
> ⚠️ **Tooling caveat — Telegram NOT sent.** The Cowork Linux sandbox ran out
> of disk mid-run (I copied the 4.3 GB `historify.duckdb` to read it; four
> identical `no space left on device` failures afterwards, so I stopped
> retrying). No shell for the rest of the run ⇒ no Fernet decrypt of the bot
> token, no HTTP. **Read this file directly.** DuckDB freshness and git state
> below were captured *before* the shell died; `strategy_runtime_override` /
> `strategy_mode` / `job_run` could not be queried and are inferred from
> file-based evidence (EOD reports, `errors.jsonl`).

### Yesterday's deltas

**2 commits on 2026-09-16** (branch `dev`, head `cf767c202`):

| Ref | Commit |
|---|---|
| `76430b246` | `[#730] feat(open15): price the watched-break counterfactual on the risk monitor's batched quote poll — live at the 09:30 exit, bars as fallback` |
| `cf767c202` | `Merge pull request #731 from sonawanedhiraj/feat/730-open15-watched-quote-poll` |

**Flagged for awareness:**
- No `WIP:`, `fix(test):` or `refactor:` commits in the window.
- #730 supersedes the #728 bars-only pricing path from 15 Sep — the watched-break
  counterfactual is now priced **live on the risk-monitor quote poll**, with bars
  only as fallback. First full day of that behaviour was today.

### Working tree state

**113 entries in `git status --porcelain`** — but only **2 tracked modifications**:

- ` M .gitignore`
- ` M strategies/simplified_engine/LEARNINGS.md`

The other ~111 are **untracked** research/backtest scratch:
`backtest/options_open15/*` (~18 scripts + `data/iv_history.parquet`,
`july_fetch_cache.json`), `backtest/open15_missed_days/`,
`backtest/open15_rolling/`, `backtest/inhouse_scanner/r60/` +
`summary_i4.json` + `trades_i4.parquet`, `backtest/news_event_study/*`,
`audit/open15_replay_removal_backup_20260817_165557.json`, `.claude/launch.json`.

Nothing load-bearing is uncommitted. `strategies/simplified_engine/LEARNINGS.md`
is the one to look at — it is a canonically-tracked file per CLAUDE.md.

### Pre-market freshness (read post-close)

**sector_follow_cap5_vol — the intraday gate inputs are FRESH.**

| Feed | Interval | Verdict |
|---|---|---|
| 30 `LOCK_STATIC_30` universe stocks | `1m` | ✅ 30/30 present, all max ts **2026-09-17 15:14 IST** |
| 30 universe stocks | `D` | ✅ 30/30 present, all **2026-09-17** |
| 8 mapped sector indices (`NIFTY`, `FINNIFTY`, `NIFTYPVTBANK`, `NIFTYPSUBANK`, `NIFTYIT`, `NIFTYAUTO`, `NIFTYMETAL`, `NIFTYFMCG`) | `1m` | ✅ 8/8, all **2026-09-17 15:29 IST** |
| 6 of those 8 sector indices | `D` | ⚠️ stale since **2026-06-04** (~105 days) — only `NIFTY` and `FINNIFTY` are current |

On the index-`D` gap: `data_freshness_service.check_strategy_data_ready` defaults
to `interval='1m'`, and sector_follow's sector gate reads index **1m**, so this
does **not** gate entries today — the strategy's own health check is green. But
it is a real 105-day hole in stored daily index history and would silently break
any *research* that reads index `D` (a backtest, a resample-derived daily). Worth
an issue; not a market-open blocker.

Also noted while querying: duplicate index symbol spellings in `market_data`
(`NIFTYOILANDGAS` vs `NIFTY OIL AND GAS`, `NIFTYCONSRDURBL` vs
`NIFTY CONSR DURBL`, etc.), the spaced variants frozen at 2026-05-29 (`1m`) /
2026-06-04 (`D`). Dead aliases, not consumed by the 8-index map.

### Scheduler / overrides

**Both sandbox strategies ran today — scheduler confirmed armed and firing:**

- `sector_follow_cap5_vol` — EOD report generated **15:30:00.77 IST**. 1 signal
  (TMPV, NIFTYAUTO +1.30%), entry ₹317.40 × 157 = ₹49,832 notional, **OPEN at
  EOD → T+1 exit 2026-09-18**. Kill switch **inactive**.
- `futures_follow_cap50` — EOD report generated **15:30:00.09 IST**. 1 NIFTY lot
  (`NIFTY29SEP26FUT` @ ₹23,328.90) on the TMPV signal, ₹250,000 overnight margin
  against the ₹500,000 (50%) cap, **OPEN at EOD → T+1 exit 2026-09-18**.
  Kill switch **inactive**.
- Mode for both: **sandbox**.
- `scanner_comparison_eod` (15:45) — not directly verifiable without `job_run`;
  no error logged at 15:45.

**No evidence of an active pause / kill-switch override** (both kill switches
report `inactive`, both strategies placed entries). Not a positive confirmation —
`strategy_runtime_override` was unqueryable after the shell died.

**⚠️ Two carried-open T+1 positions into 2026-09-18** (TMPV equity CNC + 1 NIFTY
future NRML). The NRML lot has no MIS square-off underneath it — the #497 shape.
Confirm the 15:10 / 15:25 exits actually fire tomorrow.

### Errors today (24 in `errors.jsonl`)

| IST | What | Read |
|---|---|---|
| 08:22–08:23 | Zerodha WS / connection-pool errors ×16 at boot | Reconnect churn at startup; feed came up (15:29 bars exist) |
| 09:10 | `open15: 4 watched symbols have NO NFO option contracts and were DROPPED — SCANNER_SYMBOLS is stale: DALBHARAT, EXIDEIND, NUVAMA, SAMMAANCAP` | #647 fail-open working as designed; the env list needs pruning |
| 09:18 | `scanner smoke check 09:18 FAILED: scanner_universe_1m stale; scanner_universe_D stale` | **Recurring** — same message on 09-10, 09-15, 09-16, 09-17 (≥5 occurrences in the last 1000 error lines). Arms the #390 post-hold |
| 09:30 | `scanner_dry tripwire WARN` — `last_inhouse_at 2026-09-16T15:25`, `gap_min 61.8`, `chartink_has_rows_since_cutoff: False` | The in-house scanner produced **no hits all morning**; consistent with the smoke-check hold |
| 15:05, 15:20, 15:30, 15:35 | `telegram_bot_service: Failed to send broadcast to 1345069591 — NetworkError: RuntimeError('Event loop is closed')` ×4 | **Operator alerts are not being delivered.** Recurring across many prior days in the same log |
| 16:00 | `journal_reflection_service` error | Known: posts to the Cowork bridge on :5001, normally down |

**Top two to act on**, in order:

1. **Telegram outbound is broken** (`Event loop is closed` under eventlet at
   every alert time — 15:05/15:20/15:30/15:35 today, and on many earlier days
   in this log). Every EOD summary, kill-switch alert and CRIT today was
   composed and **not delivered**. That is the alerting channel the whole
   observability stack leans on.
2. **The 09:18 scanner smoke check has failed ≥5 sessions running** with
   `scanner_universe_1m stale; scanner_universe_D stale`, and the 09:30 dry
   tripwire agrees the in-house scanner is producing nothing in the morning.
   Either the #380 straggler heal or the #299 daily re-settle is not converging
   before 09:18.

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~06:45–07:30 token flush).
- Today: open **09:15** / close **15:30**.
- ⚠️ `sector_follow` entry is **15:05**, exit **15:10** (moved 2026-08-03 for the
  NSE Closing Auction Session — the SKILL's "entry 15:20" is stale).
  `futures_follow_cap50` is 15:20 entry / 15:25 T+1 exit / 15:28 watchdog.

(Dheeraj — add EOD notes below in your own time.)
