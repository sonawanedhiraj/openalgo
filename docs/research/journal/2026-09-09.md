# 2026-09-09 Trading Journal

## 08:45 IST standup

*(generated 09:09 IST — task fired late; ~6 min to open)*

### Yesterday's deltas

- **0 commits** in the last 24h. Last commit on `dev` is `99bb86803` (2026-09-07).
- Recent context (2026-09-07, for reference):
  - `99bb86803` Merge PR #710 — thread-registry normal-completion fix
  - `da7b83e78` `[#709]` fix(thread_registry): let a window-scoped loop declare its normal exit
  - `54f38eaa4` docs(strategy): R62 — simplified_engine OOS check of R61, nothing robust, net −₹19.9k
  - `8f2938190` / `17120b791` `[#704]` open15 stop-loss counterfactual + scorecard
- No `WIP` / `fix(test):` / `refactor:` commits to flag.

### Working tree state

- **96 entries** in `git status --porcelain` on branch `dev`.
  - **2 tracked files modified**: `.gitignore`, `strategies/simplified_engine/LEARNINGS.md`
  - **93 untracked** — almost entirely research/backtest scratch: `backtest/options_open15/*` (~20 scripts),
    `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/inhouse_scanner/r60/`,
    `backtest/news_event_study/*`, plus `audit/open15_replay_removal_backup_20260817_165557.json`.
- ⚠️ `strategies/simplified_engine/LEARNINGS.md` is modified and uncommitted — per CLAUDE.md, strategy-registry
  and learnings updates go **direct to dev**. Worth committing when convenient.
- Note: `.git/index.lock` could not be unlinked from the sandbox (read-only mount, expected — no git ops performed).

### Pre-market freshness

- `data_health_check` @ **08:32 IST today** (boot convergence): **`scanner_universe_1m` and `scanner_universe_D`
  both `overall_ok=0`, 216/216 symbols stale, `refreshed=0`** —
  *"216/216 (100%) symbols still stale after a completed job"*.
- **Most likely benign**: yesterday's 15:35 IST checks were clean (`overall_ok=1`, `stale_symbols=[]`), and the
  freshness predicate measures against *today's expected 15:30 close*, which cannot exist pre-open. A 100%-stale
  reading at 08:32 is the normal pre-open artifact.
- **But it is indistinguishable from a dead broker session**, which is the failure mode that matters. The
  `refreshed=0` line means the catch-up job fetched nothing. If the first post-close convergence today
  (15:30–17:00 window) does not go green, treat this as real.
- `historify.duckdb` not queried directly — the live app holds it open with a 9.8 MB WAL, and a second-process
  open would contend for the lock. `data_health_check` is the intended read-only source and was used instead.

### Scheduler / overrides

- **All 09:00 IST jobs fired `ok` today**: `sector_follow_daily_reset`, `futures_follow_daily_reset`,
  `intraday_pullback_daily_reset`, `intraday_pullback_eval`, `scanner_dry_tripwire`,
  `multi_account_login_reminder_0900`. Scheduler is armed and healthy.
- `scanner_comparison_eod` ran `ok` yesterday at 15:45 IST (3.6 s) — registered for today.
- **Active overrides: NONE.** Latest `strategy_runtime_override` row expired 2026-08-12. Nothing paused.
- **Strategy modes**: `open15_vol_breakout` = **LIVE**; `simplified_engine`, `sector_follow_cap5_vol`,
  `futures_follow_cap50` = sandbox.
- Multi-account master switch **ON**. Child `Didi-zerodha (JX8380)` auto-logged in **08:19 IST today** ✅;
  children 1 & 2 (`Swapna`, `Mai`) are disabled.

### ⚠️ Items worth your attention before open

1. **`futures_follow_cap50` carry — P0, unresolved.** Last entries were **2026-08-28** (2 BUY lots,
   `NIFTY29SEP26FUT`, `exit_price = NULL`). No entries and no exits in the 9 sessions since. The
   `t1_exit_for_carry` contract fired **P0** on both 2026-09-01 and 2026-09-03 (fingerprint `38bd638a20a9`).
   Sandbox money, so not urgent — but the contract expires 29-Sep and the T+1 exit is evidently not firing.
   This is the #497 failure shape recurring.
2. **`postmarket_review` has not run since 2026-09-03.** Missing 09-04, 09-07, 09-08 — no `job_run` rows at all
   for those dates, so the job did not fire (not a silent failure inside it). Also: the 09-03 run reported
   `llm_status = not_logged_in` — the `claude` CLI needs a `/login` on the host for triage to work.
3. **open15 is LIVE and trades 09:16–09:30.** Config was edited via UI at **09:04 IST today**:
   `instrument=atm_option`, `max_trades=3`, `margin_per_slot=₹60,000`, `trade_side=both`, rolling watch-list ON,
   profit lock ON (target ₹6,000 / trail ₹2,000), stop-loss ON (₹2,500), residual sizing ON.
   Recent real net: 09-01 −₹9,016 · 09-02 −₹8,390 · 09-03 +₹146 · 09-04 −₹4,973 · 09-07 +₹7,924 · 09-08 −₹1,466
   → **≈ −₹15.8k over the last 6 sessions.**

### Reminders

- 🔑 **Re-login Zerodha before 09:15 IST** (daily ~06:30–07:30 token flush; a separate Kite web/app login also
  kills the API session mid-day).
- Today: **open 09:15 · close 15:30** · open15 entry window 09:16–09:29, exit 09:30 · sector_follow entry
  **15:05** (not 15:20 — moved 2026-08-03 for the NSE Closing Auction Session) · futures_follow entry 15:20 /
  T+1 exit 15:25.

(Dheeraj — add EOD notes below in your own time.)
