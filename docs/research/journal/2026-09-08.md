# 2026-09-08 Trading Journal

## 08:45 IST standup

### Yesterday's deltas
5 git entries on `dev` since 2026-09-07 (3 commits + 2 merges):

- `17120b791` **feat(open15)** — stop-loss counterfactual: stopped contracts are marked to the scheduled exit + STOP-LOSS SCORECARD (#704), merged as `8f2938190` (PR #705).
- `54f38eaa4` **docs(strategy)** — R62: simplified_engine OOS check of R61 + full improvement search. Verdict: nothing robust, net −₹19.9k (#706). Direct-to-dev registry commit, as policy requires.
- `da7b83e78` **fix(thread_registry)** — a window-scoped loop can declare its normal exit (`completed` vs `dead`) (#709), merged as `99bb86803` (PR #710).

Flags for awareness: one `fix(`, no `WIP`, no `refactor:`. Both merges are properly issue-linked.

### Working tree state
94 entries in `git status --porcelain`, but only **2 tracked modifications**:

- ` M .gitignore`
- ` M strategies/simplified_engine/LEARNINGS.md` ← R62 learnings, uncommitted. Worth deciding on: the registry doc landed (`54f38eaa4`) but the strategy LEARNINGS did not.

The other 92 are untracked scratch: `backtest/options_open15/*` (~18 scripts), `backtest/open15_rolling/`, `backtest/open15_missed_days/`, `backtest/inhouse_scanner/r60/`, `backtest/news_event_study/*`, plus **10 `db/openalgo.db.bak.*` snapshots** (Jul 14 → Aug 18) that could be pruned.

### Pre-market freshness
`db/historify.duckdb` is held open by the running app (read-only attach refused from the sandbox), so this is read from the boot-convergence log instead.

**Verdict: fresh through Mon 2026-09-07 — no historical gap.** At 08:32–08:35 the boot convergence reported every universe "catching up **2026-09-08..2026-09-08**", i.e. the only missing day is *today*, whose bars don't exist yet pre-open:

- scanner universe `1m` — 216/216 behind by today only
- scanner universe `D` — 216/216 behind by today only
- sector_follow indices — 8/8 (FINNIFTY, NIFTY, NIFTYAUTO, NIFTYFMCG, NIFTYIT, NIFTYMETAL, NIFTYPSUBANK, NIFTYPVTBANK)
- sector_follow stocks — 30/30 (LOCK_STATIC_30)

The accompanying `100% still stale after a completed job` WARNINGs are the expected pre-open artifact (nothing to fetch for today at 08:35), not an overnight failure. Last persisted `data_health_check` rows: `scanner_universe_1m` / `_D` **ok** (2026-09-07 13:50 IST); `sector_follow_cap5_vol` **ok** but last written 2026-09-03 16:30 IST — see the gap below for why.

One boot note: `historify_service.wait_for_jobs: timeout after 600s with 1 job still pending` at 08:32, and the scanner arm held the boot-convergence lock for 768s. Convergence completed anyway; worth watching if it repeats.

### Scheduler / overrides

**All jobs armed for today** (app restarted 08:21 IST, log `openalgo_2026-09-08.log` live as of 08:50):

- `sector_follow jobs registered (mode=sandbox, strategy_id=1)`
- `futures_follow jobs registered (mode=sandbox, entry_mode=legacy, strategy_id=2, smoke_check=True)`
- `open15: 6 scheduler jobs registered` (arm 09:10 / first-candles 09:16 / exit 09:30 / retry 09:32 / summary 09:35 / entry-verify every 1 min)
- `scanner_comparison EOD job registered (15:45 IST mon-fri)`
- `option_liquidity EOD (15:45)`, `trading_day_funnel (15:35)`, EOD watchdog (15:14), `journal_reflection (16:00)`, `postmarket_review (17:15)`

**Active overrides: NONE.** The three most recent `strategy_runtime_override` rows all expired 2026-08-12 10:00 (smoke-check auto-pauses for futures_follow, intraday_pullback, sector_follow). Nothing is holding entries today.

Modes: `open15_vol_breakout` = **live** (real money); `simplified_engine`, `sector_follow_cap5_vol`, `futures_follow_cap50` = sandbox.

#### ⚠ Afternoon coverage gap — two sessions lost
`job_run` shows **every afternoon job last fired 2026-09-03**. The app was down before their fire time on both following sessions:

| Session | Last job fire | Implication |
| --- | --- | --- |
| Fri 2026-09-04 | 04:40 UTC = **10:10 IST** | afternoon jobs missed |
| Mon 2026-09-07 | 08:30 UTC = **14:00 IST** | afternoon jobs missed |

Confirmed by `openalgo_2026-09-04.log` ending 10:11 and `openalgo_2026-09-07.log` ending 14:01, and by today's boot line `Run time of job "open15 option-liquidity sweep (15:45 IST)" was missed by 16:36:27`.

Did **not** run on Sep 4 or Sep 7: `sector_follow_preentry_refresh / smoke_check / entry / exit / eod_summary`, `futures_follow_smoke_check / entry / exit / eod_watchdog / eod_summary`, `intraday_pullback_eod_flatten / eod_summary`, `scanner_comparison_eod`, `option_liquidity_eod`, `trading_day_funnel`, `multi_account_eod_summary`, `multi_account_login_reminder_1500`, `postmarket_review`, `sector_follow_data_health`.

Morning jobs were unaffected both days (open15 armed, entered and exited normally). All missed jobs are sandbox/observational except the EOD reporting — no live money was stranded — but two days of sector_follow/futures_follow signal data and two `postmarket_review` verdicts are simply absent.

**Action: keep OpenAlgo up past 17:15 IST today** so the afternoon chain and the post-market review actually run.

#### Child account token dead (2026-09-07 14:01)
`errors.jsonl` ends on repeated `403 Forbidden ... Incorrect api_key or access_token` from `/portfolio/positions` via `/broker_accounts/api/open15_status` — a **child** account session, not the primary. Child accounts need their own daily Kite login before 09:15 or their open15 mirrors won't place.

### open15 live P&L (net, real fills only)
| Date | Real | Net | Other |
| --- | --- | --- | --- |
| 2026-09-07 | 3 | **+₹7,923.61** | 2 sim −₹1,422.10 |
| 2026-09-04 | 2 | −₹4,972.54 | — |
| 2026-09-03 | 3 | +₹146.02 | — |
| 2026-09-02 | 3 | −₹8,390.31 | 2 paper −₹3,511.91 |
| 2026-09-01 | 3 | −₹9,016.38 | 1 sim +₹16.49 |
| 2026-08-31 | 2 | −₹10,562.57 | 1 paper +₹20,014.70 |

Last 6 sessions net real: **−₹24,872**. Sep 7 was the first green day since Aug 28. Config in force: `atm_option`, 3 slots, ₹60k/slot, both sides, entry cutoff 09:29 / exit 09:30, stop-loss and profit-lock rows present in `open15_config`.

### Reminders
- 🔑 **Re-login Zerodha before 09:15 IST** (daily token expiry). Primary looks alive — master contract downloaded 108,544 records at 08:22 and the WS feed subscribed the full universe — but **child accounts were 403 at 14:01 yesterday**; log them in on `/accounts`.
- Today: open **09:15** / close **15:30** / open15 arm **09:10**, exit **09:30** / sector_follow entry **15:05** (hard-clamped ≤15:10 for CAS) / futures_follow entry **15:20** / postmarket_review **17:15**.
- Standup was READ-ONLY: no commits, no DB writes.

(Dheeraj — add EOD notes below in your own time.)
