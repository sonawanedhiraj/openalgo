# 2026-07-03 Trading Journal

## 08:45 IST standup

**Headline:** OpenAlgo is UP (recovered after the 08:09 "possibly down" alert — today's log + all DBs writing at 08:48–08:50). **Two things need eyes before 09:15:** (1) the `.git` directory is CORRUPTED and git commands fail, (2) scanner universe showed 216 stale 1m/D symbols after the overnight 3 AM token expiry — confirm it self-heals after re-login.

### Yesterday's deltas
14 commits since 07-02 08:48 on branch `fix/301-futures-follow-today-pnl` (tip `5443c827`, a merge of origin/dev at 08:48). Heavy scanner-reliability push:

- `6797bf40e` [#317] fix(dashboard): normalize simplified_engine last_trade_at to naive-UTC
- `f748b8245` [#297] fix(strategies-ui): LLM Decisions "Invalid Date" + on-demand LLM health chip
- `b2a307c8f` [#313] fix(sector_follow): verified post-job refresh reporting for backfill convergence
- `1e95c4699` [#314] feat(scanner): feed daily-D re-settle into broker prev-close registry
- `8aeba54eb` [#305][#304] docs: PARAMETER_LOG entries (reference-check, smoke-block, catch-up cap)
- `5d146cef6` [#305] feat(scanner): reference-data certificate + broker prev-close cross-check + smoke-fail post-hold
- `6d0b50c4f` [#307] infra: scanner PR guardrails (warn-only semgrep, bug-fix template, pre-fix regression CI)
- `dd305f0ac` [#306] test(scanner): golden incident-replay harness + production-shape frame factory
- `c349721da` [#304] fix(scanner): backfill catch-up range from last-stored date
- `916f09c33` [#301] fix(dashboard): futures_follow Today P&L ₹0 despite T+1 sell
- `24c4d23d2` [#303] fix(test): pin smoke-check entry-gate assertion to the simulated clock
- `411884afd` [#301] fix(dashboard): attribute futures_follow T+1 exit P&L to fill date
- `b476e8612` [#299] fix(scanner): re-settle provisional daily-D closes + show signal date
- `5443c8276` merge origin/dev → fix/301

Awareness flags: `24c4d23d2` is a `fix(test):`, `dd305f0ac` is `test(scanner):` — test-only churn, no `WIP` commits.

### Working tree state
43 uncommitted entries on `fix/301-futures-follow-today-pnl`: 33 modified tracked files (scanner services + rules, sector_follow backfills, dashboard API/frontend, LEARNINGS, CLAUDE.md, PARAMETER_LOG, and ~13 test files) plus 10 untracked (7 journal `.md` files from 06-29→07-03 never committed + 3 rotated `log/*.log` files). This is a substantial in-flight working set — Dheeraj's #299–#317 scanner work is sitting uncommitted. Nothing looks accidental, but it's a lot of unbanked change.

### 🔴 `.git` CORRUPTION (needs manual cleanup)
`git` commands fail with `fatal: bad config line 312 in file .git/config`. Inspection found:
- `.git/config` has a block of null bytes injected after the last branch entry (filesystem-level corruption, classic crash/partial-write signature).
- `.git/HEAD` is TRUNCATED to `ref: refs/heads/fix/` (no branch name).
- 6× `HEAD.lock.bak.*` + 2× `next-index-*.lock` files — recovery artifacts from colliding concurrent git operations (the CLAUDE.md-warned same-checkout deadlock). A merge landed at 08:48:41 as this task ran, so something was actively committing into this checkout.

I recovered the standup data read-only via a clean temp GIT_DIR; **the real `.git` was NOT touched** (audit policy = read-only). Recommended manual fix when free: repair `.git/HEAD` (`echo 'ref: refs/heads/fix/301-futures-follow-today-pnl' > .git/HEAD`), strip the null block from `.git/config`, remove stale `*.lock`/`HEAD.lock.bak.*` files, then `git status` to confirm. Until then the boot dirty-check and any automated git flow will error.

### Pre-market freshness
- **historify feed: LIVE** — `historify.duckdb.wal` written 08:48; app writing normally.
- **scanner_universe_1m / _D: STALE** — overnight convergence at 03:18 IST reported `ok=0, 216 stale symbols` on BOTH intervals (broker token expired at ~3 AM, so the backfill couldn't fetch). Both were `ok=1` at 07-02 17:10. This is the expected post-expiry state; the boot + periodic convergence should clear it after Zerodha re-login. **Confirm scanner_universe is fresh before the scanner evaluates** (else stale-D gap-signal risk, the #299/#305 class).
- **sector_follow_cap5_vol:** last health check 07-02 11:00 (`ok=1`); no fresh overnight check recorded yet.

### Scheduler / overrides
- **Scheduler:** APScheduler uses an in-memory jobstore (no DB table), so live job state can't be introspected from here. App is running (logs + DBs written 08:50); sector_follow + scanner_comparison_eod jobs register at boot per code.
- **Overrides:** the only `strategy_runtime_override` row is a sector_follow `pause` that **expired 2026-07-01 10:00** → INACTIVE. **No active pause/kill-switch.**
- **Modes:** `sector_follow_cap5_vol` = **LIVE** (real orders at 15:20), `simplified_engine` = sandbox, `futures_follow_cap50` = sandbox.

### Reminders
- 🔑 Re-login Zerodha before 09:15 IST (daily 3 AM token expiry) — and confirm scanner_universe convergence clears the 216 stale symbols after login.
- Today: open 09:15 / close 15:30 / sector_follow entry 15:20 (LIVE mode).

(Dheeraj — add EOD notes below in your own time.)
