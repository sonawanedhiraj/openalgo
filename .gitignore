venopenalgo
.venv/
.env
openalgo.db
*.db-shm
*.db-wal
*.db-journal
.env
.flaskenv
*.pyc
__pycache__/
instance/
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/settings.json
.claude/settings.local.json
config.ini
.venvv
.cloudflared/*.json

#DB Files, CSV Files and Log/Tmp Files

# Temporary/debug files
tmp/

# Runtime / logs
backtest/scans/

# Local launch scripts (operator-specific)
launch.vbs
launch_openalgo.hta
run.ps1
start_openalgo.bat
start_openalgo_task.xml

# Diagnostics and backtest results (transient)
backtest/_diag_auth.py
backtest/results_*.json

# Outputs directory (transient)
outputs/

# Numba/LLVMLITE cache directories (auto-created on Linux)
.numba_cache/
.llvm_tmp/

*.egg-info/
*.db
*.duckdb
*.duckdb.wal
*.sqlite
*.log
*.jsonl
*.tmp
*.new
checkpoint.txt
checkpoints.json


# Strategy encryption key
db/strategy_encryption.key

# DB backup files (created by upgrade/rotate_pepper.py and other migrations)
db/backups/

# CSV Files (except specific data files)
*.csv
!symbols.csv
!all_symbols.csv
!data/qtyfreeze.csv

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Node modules (all directories)
node_modules/
**/node_modules/

# Build output
build/
.next/
out/

# Frontend specific
frontend/.cache/
*.local

# Tailwind CSS output
# **/output.css

# npm cache
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Windows NUL file
nul

# Self-hosted CI runner secret (PAT / registration token) — never commit
ci/runner/.env.runner

# sector_follow_cap5_vol Day-N EOD reports — observational, not source
strategies/sector_follow_cap5_vol/eod_reports/
# Agent/boot diagnostic logs (transient; real logs are openalgo_*.log / errors.jsonl)
log/_*
log/boot_*
